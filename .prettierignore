dist
coverage
node_modules
package-lock.json

data/source
fixtures/source
data/municipal-elections

# Deterministic compact browser shards; regenerate with
# npm run export:economic-context-browser.
public/data/economic-context

# Exact Drive transport. Its byte identity is verified by the source tests;
# formatting would destroy the recovered artifact's canonical SHA-256.
docs/research/92M-campaign-finance-ethics-lobbying.json

# Generated place export; regenerated by npm run export:life-places.
src/simulation/national-places.generated.ts

# Generated by source export commands; regenerate rather than hand-format.
src/simulation/campaign-compliance.generated.ts
src/simulation/office-qualifications.generated.ts

# Generated by npm run export:place-county-relations. Regenerate the compact
# runtime index rather than allowing a whole-repository formatter to rewrite it.
src/simulation/place-county-relations.generated.ts

# Generated by npm run corpus:prose. Edit the production bank or the generator
# and regenerate; never hand-format the artifacts.
docs/prose-inventory

# Written by `npm run corpus:prose -- anchors` with ONE ISSUANCE PER LINE, and
# that layout is load-bearing rather than cosmetic: two branches issuing the
# same anchor id to different sites then write differing text at the same line,
# which Git reports as a conflict instead of composing the two claims into one.
# Reflowing it would restore the silent composition this file exists to prevent.
scripts/prose-corpus/computed-anchor-ledger.json

# Content-addressed education catalogs: regenerate with scripts/source/export-education.ts.
# Sixty megabytes of generated JSON in one chunk; formatting them builds an AST
# that exhausts the heap. Their integrity is enforced by their digest, not by a
# formatter — see tests/source/education-catalog-integrity.test.ts.
public/education/catalog-*.json

# Generated by npm run export:district-identities. Identity projection of the
# Gazetteer corpus; regenerate rather than hand-format.
src/districts/identities.generated.json
src/districts/place-membership.generated.json

src/simulation/municipal-governments.generated.ts
src/simulation/municipal-capacity.generated.json
src/simulation/municipal-rule-registry.generated.ts

# Double-clickable macOS launchers. These are shell scripts, and Prettier has
# no shell parser, so it reports them as an error rather than skipping them.
*.command

# Generated by `npm run measure:appearance-v2`, and asserted byte-for-byte by
# its own test. Prettier pads markdown table columns, which the generator does
# not, so formatting it would put the committed report permanently out of step
# with a fresh measurement.
docs/agent/evidence/modular-gen14/APPEARANCE-RECIPE-V2.md

# Desktop shell derived output.
desktop/staged
desktop/release-artifacts
desktop/controller-release-artifacts
desktop/node_modules

# Generated by npm run coverage:nationwide-rules; its test compares a fresh
# regeneration byte for byte, so regenerate rather than hand-format.
docs/systems/nationwide-rule-coverage.json
docs/systems/nationwide-rule-coverage.md

# Vendored third-party source kept byte-identical to its pinned upstream.
desktop/private-controller/vendor/

# External private-pack originals: checksum-verified inputs, not source owned by
# this formatter. Reformatting them would invalidate their preserved lineage.
# The implementation of their pipeline remains checked under scripts/.
art/authoring/
art/manifest/character_candidate_modular41_heads.json
art/manifest/character_candidate_modular45_registry.json

# Generated by npm run compile:map-geometry / compile:map-membership. Compact
# lazy browser packs; regenerate rather than hand-format (formatting would
# multiply their size and break the manifest digests).
src/maps/geometry
src/maps/data/*.generated.json

# ChatGPT's DEPTH1 package, kept byte-identical so its own SHA256SUMS verifies.
docs/research/chatgpt-answers/2026-09-22-depth1

# Byte-identical first-party captures. docs/reference/regional-opening/README.md
# states plainly that the originals remain unchanged under raw/, and
# retrieval-receipt.json records each one's exact byte length and SHA-256.
# Reformatting them would put every recorded hash permanently out of step with
# the file it attests. Several are also malformed enough that Prettier cannot
# parse them at all, which is what a verbatim capture of somebody else's page
# looks like.
docs/reference/regional-opening/raw/

# Generated by npm run export:state-voting-context, which runs inside
# npm run build:steps. Fifty per-state files of about 8,250 lines each;
# regenerate rather than hand-format.
public/data/state-voting
docs/research/chatgpt-answers/2026-09-22-depth2
