# PR85 post-M3 current-main reconciliation

Authority: owner's final reconciliation packet; preserve accepted P85D/P85E
repairs and human replay acceptance. Leave PR85 unmerged.

- Accepted repaired head: `df2844bea1ef66ffc532fd2274baa71952f23d12`.
- Fetched main/M3: `25b7e7a291e22374566c30d31552dcc4d8314d51`.
- Isolated worktree: `/private/tmp/pg-pr85-post-m3`, same PR branch in a
  separate Git administrative repository; paused source workspaces remain untouched.

Plan: ordinary-merge current main, take the judicial ownership test entirely
from main, preserve composed transition dispatch and residence/governing-role
separation, then run focused regression/browser proofs, ownership suites, full
validation and art gates. Verify mechanical-only resolution and remote heads,
commit and normally push the same branch; require exact-head CI/browser success.
No campaign design, prose, balancing, owner replay, or new mechanics are authorized.

## Mechanical reconciliation proof

The only conflict was appended audit sections. Resolution preserves the repaired
head's entire audit file and appends main's entire Civil Service / Labor audit.
The automatic acceptance-document merge retains both accepted changes. Every
other incoming path equals main byte-for-byte, including the M3 judicial
ownership test. Adult-life, life-story, campaigns, life-places, campaign
integration tests and campaign browser proofs equal the repaired head exactly.
No campaign semantics, prose, balancing, or mechanics changed.

## Verification

- Focused regression and ownership set: 14 files, 199 tests passed.
- Campaign, legislation, narrative-life and cross-tab persistence browsers:
  30 initial passes; one six-scenario legislation test exceeded 30 seconds.
  Its unchanged rerun passed with a 120-second runner budget. Actual pointer
  and keyboard activation of Work after reload is covered.
- Full `npm run validate` was run. Format, lint and typecheck passed. Unit
  tests produced 2,558 passes and 30 timeout failures under concurrent load.
  Reduced-worker retries were interrupted after the shared disk filled and
  test loading failed. The affected master-inventory suite independently
  passed all 12 tests. No assertions or repository test settings were changed.
- Source validation passed all 13 domains. Separate source replay and build
  attempts failed explicitly with ENOSPC / no space left on device.
- Demo, art validation, art inventory and art QA passed. No tracked art changed.
- Both ordinary and staged diff whitespace checks passed.

The normal same-branch push is followed by the unchanged full GitHub validation
and browser workflow. The final task report records the exact head, CI result,
proof URL and unmerged status. Local environment failures are not local passes.

LEARN: M3's frozen ownership range and the existing P85D caller-level tests are
already the smallest durable mechanisms for these recurring integration risks.
Preserve them verbatim; no new workflow, test policy or larger prompt is needed.

## Main advanced during exact-head CI

The M3 reconciliation head `b65220861d5e364d51f8393857e6d3c0270cd79d`
passed full unmodified CI: 149 files / 2,588 unit tests; browser gate passed
with 267 first-pass tests and one unrelated viewport test passing on retry.
Run: https://github.com/lamontaes/Political-Game-Git/actions/runs/34083412656.

The final fetch found accepted PR111 newly landed at
`87b9206e2c7e8721792c136afa1d7911bc100db1`. A second ordinary merge takes
all 18 incoming paths byte-for-byte from that current main, without conflicts.

PR111 changed formal seat-count metadata from a number to `FormalSeatCount`.
The existing candidacy adapter already passed the value through; its declared
type and display consumer now follow that accepted shared contract. Known
counts use the unchanged wording with `.value`; unknown counts use main's
existing `.note` verbatim. No new player-facing string is authored. Tests now
assert unknown without a numeric fallback and preserve the known-count wording.
The campaign browser metadata assertion follows the same accepted note.

This is schema-consumer reconciliation only. Campaign effects, eligibility,
time transitions, result/seating logic, residence and Work/reload activation
are unchanged. The P85D integration test and its browser block remain exact.
The judicial ownership test still equals canonical M3/current main.

Current-main focused regression/ownership set: 14 files / 200 tests passed.
All 9 campaign browser tests passed. Typecheck passed. Full local validation
was attempted again: format, lint and typecheck passed, then Vitest startup
failed explicitly with ENOSPC. The final exact-head CI must repeat the full
unmodified gate before the landing verdict. The final task report carries its
immutable head and run URL; no merge is authorized by this run.
