# PLAYTEST34 B — ordinary people and visible appearance edits

User-authorized current PEOPLE section read in full. Base private5264f205;
public source4947b7d0 inspected only for context. A owns composition/shared root;
B owns constructor/default/appearance controls/labels and canonical drawing.
V owns additive private engine-people34 paint refinement with original
engine-people29 bytes, masks, anchors and saved versions unchanged.

Trace ordinary New Game in two explicit towns and both stated player genders,
record generated identities and kinship, compare actual complete rendered people.
No exact owner mother seed/save is available; distinguish a generated reproduction
from the reported individual. Names, pronouns and Census source sex cannot be
used to infer gender/ancestry. Recorded gender may guide a fresh visual default;
explicit body choices remain independent. Palette/feature variety is seeded art
variation, not estimated demographic weights. Audit existing name/background
fields separately without new surname-to-race rules.

New constructor behavior receives a new replay initialization marker; old
markers, unmarked records, seeds/catalogs/choices and material sources stay exact.
Body edits show the pending selection and a viewport-bounded complete-person
preview with Apply/Cancel before one validated canonical write. Plain skin,
hair, clothing style/color labels replace raw internal IDs. Test pointer,
keyboard, viewport, cancel, save/reload and portraits across actual surfaces.

Private assets/publication hold unchanged. Heavy test slot is held by A until
released; light implementation proceeds. Final evidence outside tracked banks,
exact handoff and remaining visual limits required. No new renderer/fitting.

Implementation trace: NewGameSetup → createNewGameWorld/buildProductionWorld →
createStartingPerson and generatedIdentityFor household records → saved Person.identity
and derivePersonAppearance → marked prepareCandidateOpeningWorld →
initializeFreshCandidateOutfits → resolveCompleteOutfit → saved appearance selection,
material and outfit → existing render plan/ModularCharacter/PersonPortrait.
The prior v1 body draw was independent of canonical identity and always began with
first material ramps/compatible clothes. v2 uses only recorded male/female default
presentation; unstated/nonbinary/absent remain unassigned by this rule. Supported
art variation is independent of names, hometown, pronouns and ancestry.

V ec428c758b6defe7558a02d6ad75ee9cfad13689 additive subtree consumed;
72 prepared components appended in review generation6. Old generation5 source
and registry byte replays pass. Material version remains engine-people29-v1:
this is an additive source family, not a new renderer or rewritten saved material.
Native dialog keeps background controls inert and pending appearance off World.
Cancel/Escape discard it; Apply crosses existing full-outfit validator before save.
Saved caller's generic label map had overridden friendly family labels; explicit
prepared labels now take precedence. Unsupported options disclose missing complete
outfit instead of suggesting a body identity is forbidden.

Small LEARN: registration now refuses a missing frozen family before it can
publish an empty pack. Ordinary-life tests inspect canonical person records and
parent-child links, name-independence, full outfit resolution and old v1/gen5
persistence rather than proving defaults through a list of menu options.

Actual ordinary browser proof exposed and repaired two receiver seams after the
constructor checks: SceneBackdrop drew raw URLs, discarding saved materials and
features; ScenePersonLayer now carries source ID/kind/material and reuses the
existing MaterialImage with actual drawn clothing IDs. Portraits previously
framed the full-person canvas as a tiny whole body; the existing plan now crops
prepared portraits using authored crown/root anchors. No person/source change.
The saved wardrobe stage also needed viewport height bounds to show shoes above
the panel footer. Evidence compares actual room/conversation/dossier face SVGs.

Verification completed: 35 focused tests across eight files; typecheck and scoped
lint pass, final crop typecheck/lint pass; art validation, inventory, QA and release
check pass. Three actual ordinary Chromium walks pass (two adult towns plus a
synthetic child household); two final native cases pass for color/feature/outfit
save-reopen and room/conversation/dossier face-byte equality after portrait crop.
Pointer and keyboard activation, Escape/Cancel exact preservation, Apply canonical
write, 1366×768 and 390×844 modal bounds are verified. Both frozen registration
byte replays pass. Browser evidence lives in test-results/runs/p34-b-ordinary-v7
and p34-b-portrait-v8; portable copies /private/tmp/p34-b-delivery/index.html.

Acceptance: implementation and Chromium mechanical proof complete; human art
approval remains open. Native Safari is unverified (native app inspection was
slow and supplied no B game-control proof). The exact owner save/seed remains
unavailable; synthetic parent proof is not relabeled as the recorded individual.
Old saves/old source bytes are preserved. Standing adult-only art, stylized
faces/hands and inherited lower-body facets remain visible limitations. No
background replacements, training, generation, fitting project or name weighting.

A receiver: use only B's net leaf changes above private5264f205. Existing root
setupForArtPreview + prepareCandidateOpeningWorld call sites remain the complete
adapter; no root rewrite. New starts pin review6/complete-outfit-v2; v1 descriptors
and old catalog pins remain as saved. Retain C's People-node layout and consume
the shared PersonPortrait for the saved face/material/crop. Reconcile the small
SceneBackdrop MaterialImage leaf with any current layout changes. V's ec428
additive subtree is included exactly. All assets stay draft/pending/unreleased.
Public distribution remains under the existing approval hold; no upload occurred.
B released the actual heavy host slot directly to LIFE after every process exited.
