# PR79 current-main integration (79B)

Authority: user-authorized 79B handoff, Drive `1YUcAo5sIjO0Ze7rFdfH5IR7Xcmbin2SBGXb9rGH85Xs`.

Preflight: isolated `/private/tmp/pg-pr79-integration`, existing branch `claude/legislative-bargaining-dialogue-realism`; clean starting head/upstream `b8a770807cb9093839154fd205c8447ef9cda354`. Main fetched at `1a91101ab4f5369aeec21c6e9f32c21114794c81`; PR85 open at `df2844bea1ef66ffc532fd2274baa71952f23d12`; PR111 open at `fdaaf2a857f564e806e102c0c27a23f78bd31b30`. All referenced PR heads (79, 85, 111, 118, 113, 89, 101, 82, 86) fetched.

1. Ordinary merge on existing branch. Preserve main decisions and M1 frozen ownership guard; append bargaining decisions as D-081/D-082 (D-078/D-079 reserved by live PR89; D-080 reserved by the handoff for PR101, whose fetched branch still carries stale D-078).
2. Adapt bargaining response, relationship and canonical writes to subject contracts, preserving repaired modules and tests.
3. Add ordinary-subject before/after record proof; run focused tests, deterministic demo pair, validation, full Playwright and art checks.
4. Publish with remote-head verification; run exact-head CI; leave PR79 open for independent acceptance.

PR85 route remains a follow-on after its merge: build from the player's World, governing seat/chamber, canonical measure and people; fail closed without content; never import fixture state into real saves. Residence/governing-jurisdiction and no-fixture-leak tests belong to that route handoff.

Live-main discrepancy: M1 file contains nine FORBIDDEN patterns, not the handoff's eleven. Preserve the fetched file's actual guard and frozen ranges.

## Implementation result

Main advanced during work to `333eeb12b3df4322aaebe6dfa987a653bf143223`;
that update merged cleanly after the initial reconciliation. No salvage branch
or PR was created. The existing PR79 branch remains the publication target.

The subject registry supplies its response, progress and resolved commit hooks.
One optional world-writing consequence hook runs after canonical turn evidence
exists. Ordinary subjects omit it. Bargaining's repaired domain and motif
modules, both original focused regression files and its four browser scenarios
remain byte-identical to `b8a7708`. The floor view needs one additive call-site
update for main's required dynamic-surface projection.

The current-main scene test exposed a second painted desk title during merge
verification. The default entry is now empty as main requires; explicit caller
labels still render. No test was weakened, retimed, skipped or deleted.

## Validation evidence and acceptance

- Focused bargaining, standing, Run B, conversation-commit and new ordinary
  replay proof: 103 passing tests across five files. The standing file actually
  expands to 25 cases on the fetched head, beyond the handoff's older count.
- Before/after replay: complete relationship, commitment, aftermath, event and
  returned-turn digests match main `1a91101`, including a scheduled callback.
- Two `npm run demo -- validation-seed` outputs match byte for byte; SHA-256
  `23577a0dbaf87b9f126d63017b5f75ab8e6db24854aa9711fe5a010bf7aa6cfd`.
- `npm run inventory:art`: current, 322 items. `npm run qa:art`: completed with
  no tracked asset changes.
- Original four bargaining browser cases and new keyboard activation proof
  passed during the focused browser run. Main's scene geometry tests also
  exercise actual pointer and keyboard activation.
- Full `npm run validate` and Playwright execution are release checks. The PR79
  acceptance request records their final results and exact published SHA. CI
  explicitly checks out the PR head, with full history for frozen-range tests.

Implementation completion is not independent or human visual acceptance. PR79
stays open and unmerged. The PR85 player-route handoff remains exactly as
described above, with no synthetic measure or fixture World substituted into
a real save. No campaign/election or rule-pack files were edited in this task.

## LEARN

A clean textual merge can still break a caller contract or restore an obsolete
visual default. Preserve negative assertions such as the empty desk hit target,
compile every reused scene consumer, and compare complete canonical ordinary
turn records across a subject-hook change. Durable mechanisms: the existing
scene co-registration tests, the new keyboard integration test, and the pinned
ordinary-conversation replay regression.
