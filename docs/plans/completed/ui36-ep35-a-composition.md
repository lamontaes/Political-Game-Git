# UI36 + STYLE35 A composition

Status: completed private playable reception. A is the sole receiver/root integrator; both original Fable source workspaces and their tracked/untracked inputs remain preserved and read-only. This does not claim main landing, installation or human visual acceptance.

Base: clean playable `2b8237a19dd1eceaa43ba96eb186327e0b224349`. Receive committed UI36 `d8fcd00bf77093a70af921ae8bef6d5a8db25efe` (two frontend commits) and STYLE35 `da6344d23797f6190471fe828c23fa71bd800a6f` only; leave their ongoing tracked/untracked edits alone. Receive LIFE#247 named-browser evidence without rebuilding its runtime.

Preserve birthday/town, accepted tuition/grace, structured Press, canonical World/content-pack Options, actual activity time, B's current appearance transactions and all old generation pins. Admit ep35 embedded PNG candidate parts through the frozen loader, with unknown rights/private distribution hold unchanged; verify the actual provider, not merely a worker page.

Reconcile frozen frontend against newer root adapters instead of replacing them with its older public base. Keep one person card, preserved browse/exchange/focus and wired calendar interruption preferences. No second frontend, clock, election evaluator or new jurisdiction rule.

N/X#248 dated-term defect and missing ordinary candidacy are a separate minimal owner follow-up, not a gate on UI/art. Do not equate recorded result time with office-term authority or invent an appointment/producer.

Verify changed-file hygiene and normal build/type, focused shell/material/save/clock refusals and two ordinary pointer/keyboard journeys with actual provider/save/time proof. Keep existing guards and retain failures. Run required art3 once on frozen source, report exact command budgets. Do not label local/private reception as main landing, installation, native or human acceptance.

Primary delivery uses a new separate origin/profile; preserve prior5275/5274/BF9 and installed/last-good saves. Hand Desktop the exact combined source and real internal-review packaging artifact, not the production profile or two separate worker sources.

Resource checks begin only after S's actual direct release; T ACKed S→A→T, then T→K→Desktop→N original-guard proof. Light assembly proceeds now; no generic process kills, duplicate owners, private export or mandatory all-feature batch.

Delivered compiled internal-art-review source `b184567e6fee67c9e9705f213b3eaa8527d0f616` at `http://127.0.0.1:5276/?art-preview=candidate`, tree `fb15f67fbaf886ddf61d6ad6db7f88a332fdbc2e8875576eeae4556bc3c0f598`. Desktop independently validated the exact target. Later scripts/test/docs commits do not change app/art and must not relabel its build stamp.

Actual build/type/art3/scoped hygiene and focused50+4 units passed. Ordinary wardrobe Cancel/keyboard Apply/save-reopen/narrow journey passed; Mom saved-proposal30-once passed on identical runtime. Compiled Maya/Aurora native News→keyboard Return preserved the same conversation; Escape restored quiet rest/person focus. Independent helper Alpha/Beta differing saved dates survived switching and full reload. The corrected automated nonKY rerun still timed out in initial page.goto30000ms; retain that and prior harness/select failures without waiver. Full actual receipt: `/private/tmp/our-civic-duty-ui36-ep35-a-delivery.md`.

LEARN: Mac native selects need real typeahead/Tab commits; do not call synthetic arrow attempts a runtime patch. Distinguish two-life isolation from same-save reopen, and audit SHA from binary source. A check batches exited and directly released K; subsequent finite Desktop/N/F work is separately owned, not this completed composition's gate.
