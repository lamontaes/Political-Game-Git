# C119D instance/cast identity repair

Authority: C119D Drive packet `1el3lLV1I_ccWVPZAaV17Va1QZ5-Km730MDn22IbW5jM`
and C119C rejection `1G6amG4VK04Y4JmID_qB1GrIDqcgx_wEwMMu4aUG6ybg`.

Starting head: `fc3574c0264f3337b8cc4317861891a27683b376`, PR #119.

## Bounded plan

1. Reproduce two-candidate persistent identity versus age-qualified cast divergence.
2. Withhold mismatched persistent-role stages in the shared eligibility seam;
   retain the existing write-boundary revalidation, schema and stable keys.
3. Prove matching-person callback/save/replay, mismatched pact rejection and
   equivalent sibling coverage without changing production eligibility supply.
4. Preserve C119C grounding, withholding, prose and accounting controls.
5. Ordinary-merge latest main, run focused/full validation and browser proofs,
   push to the existing PR branch without force, verify exact-head CI.

Research accounting remains 16 registered kernel IDs out of 62, with 46 deferred;
20 registered scenes include ten explicitly withheld scenes. Synthetic test
coverage is not a claim of ordinary runtime availability or owner acceptance.

## Implementation and validation evidence

The shared guard is implemented without changing schema, stable keys, age gates,
content or prose. Before the guard, the new pact and sibling regressions both
failed: key `person_286cd021a17467bc` versus pact cast
`person_286cd321a1746cd5`; household key `person_67427f0784365a59` versus
cast `person_c98520bc631b5fc8`. After the guard, the initial focused seven-suite
run passed 136 tests. The final regression also reconstructs the old mismatched
pact beat and proves direct writing rejects it.

The three affected triplets passed deterministic grounding and stored reviewer
verification. Art inventory and QA passed, with 322 items and no tracked churn.
Full `npm run validate` passed formatting, lint and typecheck, then reported
2,528 passing tests and six failures: five host timeouts and one ownership
assertion. No test timeout or assertion was weakened.

## Blocked by current-main integration

Ordinary merges incorporated main `333eeb12b3df4322aaebe6dfa987a653bf143223`
and then `9c36b2fb9f559db8c1cdc45ce98f7305c949f0ad`. The inherited
`tests/judicial-gameplay-ownership-boundary.test.ts` measures from
`414b24ce6120799985d3b0bddbf196c9c064df36` to the current checkout and rejects
PR119's existing `src/presentation/narrative-life.test.ts` as judicial work.
This is outside C119D's identity scope. The packet explicitly requires stopping
on an out-of-scope semantic integration conflict; no branch-local exemption or
judicial harness repair is authorized here.

The repair is retained locally, not pushed. Full validation did not reach its
source validation/replay, build, demo or art-validation steps. No new exact-head
CI or browser proof is claimed. Fresh C119E readiness remains pending resolution
of the ownership integration blocker and completion of those gates.

LEARN is encoded in the multi-candidate regressions and the architecture audit:
instance identity and the age-qualified cast must be checked together.

## Post-M3 publication

The owner authorized resuming publication after M3. Main
`25b7e7a291e22374566c30d31552dcc4d8314d51` was incorporated by ordinary merge
`5181e565bc59fc8ccaa1cd70eaabd6c4988ccf9a`. The only merge conflict was
independent appended audit sections; both were preserved. The judicial ownership
file now matches canonical main exactly. No C119D production or test code was
redesigned, and no unrelated local commit was introduced.

Post-M3 focused validation: 178 tests across nine suites passed, including
identity, grounding, narrative/content and ownership. All three affected triplets
passed deterministic grounding and stored reviewer verification. Thirteen
narrative browser tests passed on an isolated port. Inventory and QA passed with
322 assets and no tracked churn.

The local full validation passed formatting, lint and typecheck, then reported
2,550 passing tests and eleven five-second timeouts across seven unrelated
suites. A one-worker rerun also encountered host time limits; no limits or
assertions were changed. Exact published-head GitHub validation and full browser
proofs are required before reporting C119E readiness; final CI evidence belongs
in the publication response. Owner acceptance and merge remain separate.

Implementation is complete. The original blocker above is historical and was
resolved by canonical M3, not a branch-local exemption.
