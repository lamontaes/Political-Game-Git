# J1B PR #115 post-M1 reconciliation

Status: Reconciliation complete; final exact-head CI/browser acceptance is
reported separately on the task. No merge or human visual acceptance claimed.

## Authority and scope

Execute J1B (Drive `119PJI7bhmNGd5bX0A46Y7TtAprbEnhaAb5vgzeuOBBo`).
Preserve accepted feature head `59ac50983941dd6e640378adc964acd8f9fcd5cc`.
Merge M1/current main `a93b8f9da2b76d69123abc6b37b4b196f9d0d5db` using
ordinary merge and publish to the existing PR #115 branch. The isolated
worktree is `/Users/lamontae/Documents/Political-Game-J1B`; both source
workspaces remain read-only. Preflight verified the accepted local/upstream
head and no existing changes in this worktree.

## Reconciliation and acceptance procedure

- Take the inherited 92H ownership test byte-for-byte from M1/current main.
- Update only the judicial ownership base; preserve its forbidden surfaces.
- Preserve the other five accepted files byte-for-byte, including all ten
  compiled and fifty mechanic-gated rows and canonical writer delegation.
- Run focused judicial and repaired 92H tests, full validation, source
  validation/replay, build/demo, all three art commands, and browser proofs.
- Re-fetch main and the PR branch before publication. If main moved, merge
  it ordinarily and repeat exact-head checks; stop on semantic conflicts.
- Require green CI for the published head, then verify the minimal boundary
  independently without repeating the accepted research audit.

## LEARN

An inherited ownership assertion must measure its original closed packet
range after merge. Preserve M1's frozen 92H endpoints; only an open feature's
own base follows reconciled main. Byte comparisons against the accepted head
and current main provide a small, reproducible reconciliation check.

## Reconciliation evidence

The ordinary merge brought in only the inherited 92H ownership test, exactly
as M1 shipped it. All five non-bookkeeping J1A files remain byte-identical to
`59ac50983941dd6e640378adc964acd8f9fcd5cc`. The sixth file changes only its
accepted-main base; its forbidden list is unchanged. The executable bank
reports sixty rows: ten compiled, fifty mechanic-gated, ten definitions.

Focused tests passed 24/24. The default local validation passed formatting,
lint, and typecheck, then reported seven five-second timeouts in unchanged
tests. A complete rerun with two workers and a 30-second allowance passed
2,372/2,372 tests. Source validation, byte-identical replay, build, deterministic
demo, art validation, inventory, and QA passed. The unmodified CI repository
validation also passed on the reconciliation merge; final published-head CI
and browser results remain the acceptance gate, not these earlier results.

For browser proofs, use an exclusive port and refuse server reuse when other
worktrees are testing. Run repository tests BEFORE starting the browser server:
the art-readiness escape test briefly creates a symlink that Vite's art glob
can observe. Neither workaround requires tracked configuration or feature
changes. Interrupted browser runs remain diagnostic evidence, not passes.

## M2 final-main reconciliation

Main advanced during CI to `414b24ce6120799985d3b0bddbf196c9c064df36`.
The required additional ordinary merge was clean. The five substantive J1A
files and M1 ownership test remain unchanged; only the judicial ownership base
moves to this latest main. The earlier validation evidence above is historical;
this new exact head requires fresh full validation and browser CI.
