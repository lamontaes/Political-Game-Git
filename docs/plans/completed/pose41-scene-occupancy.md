# POSE41 bounded scene occupancy

The user authorized this increment on 2026-09-14 in the MODULAR41 G coordination brief. G retains scene placement. PEOPLE40 scene commit `bb0636c32495660022ef48b93c5f7ea35db0b891` and its comparison remain frozen. The private proof input commit `2d720ea909b14d80025f053eac4191e0ce8ad064` is not an integration delta.

## Scope

- Publish the frozen crop transform, source-image contact estimates and file ownership in `docs/integration/g41-scene-contract.json`.
- Receive POSE41's additive adapter and first usable pose coverage; retain B's source and wardrobe files unchanged.
- Allocate unique compatible near, middle and seated places to actual current World occupants. Selection and Talk do not influence assignment or scale.
- Fit visible source contacts uniformly, preserving body mass. Recalibrate any changed scene geometry against the retained original.
- Verify living-room and shared-workroom placement, honest missing-pose fallback, occupancy and quiet/Talk behavior at 1280×860 and 1200×720. Synthetic fixtures must be visibly labeled.
- Hand A only completed scene changes, without waiting for future wardrobe or pose scope.

## Known constraints

The workroom's only visible seat faces away; a front-facing seated guest cannot occupy it. Its other seat cushions are hidden behind tables. Canonical-03 sofa is the first front-pose candidate, pending source contact and furniture-occlusion verification. The club chair's angle is not accepted for a front pose. All room contact values are image-space visual estimates, never surveyed physical dimensions. The workroom's inherited tile convention must not be reported as a measurement.

## Preservation and verification

No World attendance, clock, identity, save, menu, Calendar, News/Journal or dialogue-system writes. No Desktop profile changes. A receives this as a separate increment after frozen PEOPLE40. Run focused placement/occupancy tests, typecheck/build, required art commands, and pointer/keyboard visual proof. Record exact SHA, dirty state, remaining gaps and human acceptance state.

## First completed increment

Received immutable POSE source71c89f14 (two exact default seated averages) as
private proof prerequisite0237d78a. G does not edit the source pack. The scene
consumer is ready for later exact variants through the same API; it does not
wait for every body/outfit/pose. A must receive the POSE-owned pack separately.

Added a unique middle-floor place, strict front-seat facing checks, single-scale
source-pixel placement, per-person capacity allocation and visible alpha
clearance. Workroom retains its original floor ramp and intended visible adult
height, removing the old canvas-aspect assumption. Three compatible unique
occupants render in both proof rooms; workroom uses standing only. Sofa contacts
fit the received source; no raster edits or new background generation occurred.

70 focused tests passed; changed-file ESLint, typecheck, production build and all
three required art commands passed. Synthetic near/middle/seated checkpoint
screenshots at1280×860 and1200×720 verify equal actor/camera rectangles in quiet
and Talk. Actual A39 replay additionally passed actor/dossier/Talk pointer and
keyboard activation, speech, Back, current “No time passed.” and stable placement.
World/appearance immutability and absent/unknown occupancy are checked directly.

Acceptance: engineering proof complete for first pack. Human visual acceptance
pending. Front workroom seated and unprovided exact outfits/poses remain gaps;
no unsupported combination is claimed complete. Expanded POSE41 v2 source remains
a separate revision. Original PEOPLE40 before/after output remains untouched.
