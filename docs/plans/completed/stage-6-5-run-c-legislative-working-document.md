# Stage 6.5 Run C Scene-Native Legislative Working Document

## Status

Implementation and local validation complete on
`codex/stage-6-5-run-c-legislative-document`, created from verified
`origin/main` at `29c3331a557e50d886d4a3b5dabc8ffe78450f92`.

## Bounded implementation plan

1. Compose a deterministic synthetic Transit Access Pilot working draft over
   the accepted Run B office fixture. Keep document, provision, variant, and
   selection identity in the React-independent presentation layer.
2. Record two prepared quantitative alternatives through the accepted Stage 6
   policy writers. Map the shared quantitative provision explicitly to distinct
   Lexington-scoped government-outlay operations for $8,000,000 and $4,000,000,
   including an explicit eligible-rider segment key.
3. Create estimates through the existing baseline, implementation-profile, and
   projected-causal-root writers. Prove that projection differs while metric
   truth, effects, realization, and law remain unchanged.
4. Gate staff interpretation through actor-specific
   `recordPolicyAnalysisKnowledge()` records. Project only analysis known to the
   controlled person, with Collins attribution and qualified forecast language.
5. Add a scene-native physical paper entry point and focused document workspace
   with real DOM legal text, stable phrase selection, restrained contextual
   actions, annotation/clean modes, and a nonmutating markup comparison.
6. Commit one duplicate-safe office instruction selecting the prepared
   $4,000,000 variant as an ordinary same-date historical event. Derive the
   active office draft from that exact canonical event without advancing date,
   consuming action sequence, realizing policy, or claiming enactment.
7. Add one authored legislative-provision subject to the existing Run B
   conversation adapter. Reuse its room, addressee, audibility, listener,
   event, claim, knowledge, and perception semantics; do not create another
   dialogue engine.
8. Add focused semantic and Playwright coverage, update affected architecture,
   decision, presentation, UX, dependency, audit, and acceptance documents,
   run the complete validation gate, and stop at a new open unmerged PR.

## Explicit boundaries

- Run C working-draft/document state is not enacted law and does not replace the
  future law, institution, legislation, appropriations, or procedure model.
- The provision bridge consumes the frozen Stage 6 quantitative policy and
  effect machinery; it adds no UI-only consequence calculation and never calls
  policy realization for preview or office-draft revision.
- Document selection, annotation visibility, clean mode, compare, paging, and
  focus state remain presentation-only.
- The only canonical Run C player writes are explicit policy-analysis review
  knowledge and the final office working-draft instruction.
- No Run D calendar/time/Work/Pending surface and no Stage 7 system is included.

## Completion gate

- All focused Run A, Run B, and Run C semantic tests pass.
- Full Vitest, TypeScript, formatting, lint, build, deterministic demo, art
  validation/inventory/QA, and Playwright pass.
- `git diff --check` and high-severity dependency audit pass when registry
  access is available.
- Architecture integrity and hidden-information audits are recorded.
- The plan moves to `docs/plans/completed/` only after implementation and local
  validation complete; the new PR remains open for independent review.

## Completion evidence

- Focused semantic suites: Run A 15/15, Run B 35/35, Run C 35/35.
- Full Vitest: 28 files and 405 tests passed.
- Browser proof: 23/23 Run A–C Playwright scenarios passed with one local Chrome
  worker; the 5/5 Run C subset also passed independently.
- `npm run validate` passed formatting, ESLint, TypeScript, full Vitest,
  production build, deterministic demo, and art validation.
- `npm run inventory:art` and `npm run qa:art` completed; generated reports were
  normalized afterward without semantic changes.
- `git diff --check` passed and the production dependency audit reported zero
  vulnerabilities.
- The full audit reported one high-severity denial-of-service advisory in the
  existing development-only `image-size` art-QA dependency. The registry and
  GitHub advisory list no patched release through 2.0.2; this Run C branch does
  not broaden that tool's input boundary.
- No file under `src/simulation/` changed. Run D and Stage 7 systems remain
  unstarted. Independent repository and human play review remain pending on the
  new open PR.

## Human-play acceptance correction

Human review of PR #10 found three bounded presentation defects. The follow-up
removes generated `review` text from the selected legal phrase, compacts the
bottom-left time/location shell only while the document is open, and moves
current/prepared/previous role labels out of immutable variant identity into the
history-derived document projection. Focused and browser regressions cover the
pre-commit and post-commit labels, shell non-overlap, retained selection
affordance, exactly one revision event, and unchanged policy realization,
effects, metrics, date, and action sequence. The accepted Run C semantic bridge
and Run A/Run B behavior remain unchanged; independent correction review and
human replay remain pending on the same open PR.
