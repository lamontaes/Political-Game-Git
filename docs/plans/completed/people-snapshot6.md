# PEOPLE-SNAPSHOT6 — saved-person presentation snapshot

Existing PR #134; entry head 303c7ad81ca16649f6a99e25b5ba146e637b6b0f,
upstream claude/people1-existing-art-assembly. Entry main
701e68ca2fab1aa8b5a69b06c8e7ada312ae2aa3.

Execute the ACCEPT-PEOPLE6 FOLLOW-THROUGH addendum. Preserve the independently
passing source, fit and identity evidence and candidate boundaries.

1. Reproduce the primary caption versus loaded outfit discrepancy on the
   unchanged saved-person review route, recording actual asset IDs and URLs.
2. Resolve one presentation snapshot for caption, full figure, portrait and
   scene; distinguish stable base identity, requested and saved preferences,
   and effective rendered outfit without rewriting appearance identity.
3. Verify selection, save/reload, outfit and person A/B/A with delayed loading
   and independently specified expected assets. Preserve refusal behavior.
4. Publish clearly named corrected full-figure, portrait and scene evidence;
   retain the old screenshot as superseded. Run scoped validation and deliver
   the exact adapter to UI #144. Same PR, unmerged, no automatic approval.

Browser reproduction is queued behind LAND-READY6's finite heavy slot.
No application changes before reproducing the discrepancy.

Reproduction complete: unchanged303c7ad route, one browser test passed in3.7s.
Fresh screenshot and loaded IDs/URLs show parka/joggers while the primary caption
prints sweater/jeans from the stable base recipe. The resolver/save behavior is
not blamed for that reproduced labeling defect.

Architecture audit: snapshot is presentation-only, uses the existing resolver,
keeps placement with each compositor, and never writes simulation identity or
history. Candidate libraries stay explicit; production callers retain existing
release/fixture gates. No Stage6/6.5 semantics or Stage7 systems changed.

LEARN: a stable identity signature is not an outfit caption. Regression proof
must compare live decoded image pixels/URLs to independently pinned source files,
not only compare two UI labels derived from the same variable. Encoded in the
new snapshot unit/browser tests and explicit base/requested/saved/effective labels.

Completed: shared snapshot/caption correction; unchanged-head reproduction;
final2/2 browser proof at52cb34eb5a395543e95392641cff151d42a08070;101 scoped
and corpus tests; typecheck/lint/format/build and required art checks. Existing
historic screenshot retained as superseded for its outfit caption only.
UI adapter handoff and published head are recorded in the delivery report.
Independent narrow replay/human candidate approval remain separate and pending.

Publication re-fetch main: `c0beb01d976fe93268827b6e7fe4f5bf29513199`; subject upstream remained `303c7ad81ca16649f6a99e25b5ba146e637b6b0f`.
