# REST37-N recovery and result-to-term continuity

N continuation in isolated `/private/tmp/pg-rest37-n`, branch `codex/rest37-n`.
Received clean prepared N `a4a0d58a660913ff73998ae2132935751ce2273a`, including
frozen `3fd95b19` national records/consumers and rival/expiry proof. No later
uncommitted N changes exist. Received S `51dfa409` and read its current dirty
multiple-seat reader/office-context contract without modifying that worktree.
Main received at `f22fd314e72bec0440044026ccdfd99e7a67600d`.

The original recovery browser check first fails before New Game: cold ESM
initialization of N's eager registry reaches world-metrics SEMANTIC_KEY before
initialization. Fix that owned integration defect first, then reproduce the
actual same-day action/day progression failure. Remove only obsolete fixed
session-count and automatic-kitchen assumptions; retain exhaustion, legitimate
next-day action and no-teleport/no-commitment-bypass/reload assertions.

Implement supported dated term entry/expiry and rival/changed-office continuity
through the canonical result/work/clock boundaries. S retains legislative
consumers and its collection/scope semantics. Keep national allocation,
certification, ballot/count, qualification and office producers distinct. No
campaign restart, UI/art redesign or all-state gate. Publish the authorized
useful source increment after exact-head verification. Human visual acceptance
and installed delivery remain separate.

## Completed implementation and software proof

Received the prepared N work before changing it; the original N remains clean
at `a4a0d58a660913ff73998ae2132935751ce2273a`. The original S remains at
`51dfa409af52fb00e9d829fab085734ec319a611`, with its prepared local work intact.
The current S reader SHA-256 is
`ddf52574286fc84793a96a9de8a718d69040f5bad8d13c6386a351f720d4eaba`.
The seam patch was applied to a disposable copy, never to S's worktree.

Actual recovery: six sessions fit the current route. The saved exhaustion
moment is 2026-01-05 20:15, and Pass day recovers to 2026-01-06 07:00.
Exhausted/recovered reload and the next actual keyboard action pass; person,
control, residence and dwelling occupancy remain unchanged. There is no
fixed session quota and no kitchen/location requirement. The owned cold-import
failure was the eager national registry; lazy construction and its compatibility
export preserve the prepared interface.

Result-to-term proof: result-day work remains expected and grants no current
member action. The supplied fictional result fixture crosses 2027-01-01 via
ordinary Work/Space, files through S by pointer/Enter, and reloads the exact
HB 401 docket record with the same sponsor and filing date. Changed workplace,
ended pending work, deceased winner, rival winner, expiry and reload are checked.
The current prepared S reader preserves two independent offices, withholds
unscoped action as ambiguous, accepts explicit scopes, and keeps the second
office usable after the first is explicitly ended and reloaded.

The shared campaign evaluator is byte-identical to received main (SHA-256
`6cd0313423f086237dc5e0e38712e4be216db1255df9bf2e80d7741bfe742d54`).
No outcome model or campaign restart was added. National allocation,
certification, ballot/count, congressional membership/choice and actual
qualification/oath producers remain the named separate obligations in the
national contract. S owns legislative consumers and date presentation.

## Verification receipt

- Full simulation/presentation: 199 files, 2,784 tests passed.
- Focused term/office regressions: initial 52 passed; the later 48-test run
  exposed an obsolete second winning-seed assumption. Its corrected supplied
  recorded-result authority fixture passes all 8 action-boundary tests.
- Anchor CLI: all 50 tests passed after deterministic `corpus:prose -- anchors`
  recorded exactly `projectCampaign-0003`; no prior ID retired/rebound, no
  issuance history rewritten. Corpus regeneration/check: zero hard errors;
  265 review warnings and 6,637 classification candidates remain diagnostic.
- Format, lint, TypeScript/production build, release check and all required
  art validation/inventory/QA commands passed. Art inventory retains its
  existing duplicate-hash warnings; no tracked art was changed.
- Identified browser `rest37-n-route-proof`: 3 passed with stable source digest.
  Strengthened exact-bill browser `rest37-n-bill-reload-proof`: 1 passed.
  Artifacts are under `/private/tmp/rest37-n-browser/`.
- The earlier whole-repository test attempt was interrupted after 9 anchor
  failures. The diagnostic identified this increment's missing prose anchor;
  the entire affected 50-case CLI suite now passes. The whole aggregate
  `npm run validate` was not claimed as executed/passed.
- Broad precommit receipts are historical command results, not final-head
  certification: source changed while they ran. Final committed-head check
  receipts are emitted under `/private/tmp/rest37-n-receipts/`, and final
  browser proof uses `rest37-n-published-head` with source held fixed.

Publication target is `origin/codex/rest37-n`; LAND retains merge/integration
and installed-delivery authority. The final response and exact-head receipts
supply the immutable published SHA and PR. No installed or human visual
acceptance is claimed. No remaining failure is known in the bounded N route;
unadmitted term/oath/special-election producers and S's remaining date-reader
mount are explicit gaps, not silently admitted behavior.

## LEARN

Ordinary cold boot catches registry import cycles that feature fixtures miss.
Keep lazy construction and the cold ordinary browser path. A changed clock/date
must not cause authority fixtures to chase a new winning seed: supply a declared
recorded result and test the actual term boundary instead. Computed wording also
requires its coupled anchor ledger and deterministic corpus refresh. Freeze
identity inputs through exact-source check teardown; never present a source-drift
receipt as certification.
