# DEV-SKILL4 — PR147 bounded follow-through

Authority: FINISH-WAVE4 shared contract and I, 2026-09-09, Drive
`1L5NDuhmPzJ5Nio8hnnfQWl6_kZqmS7uL5RjdS5toVfk`.
Source: codex/skill-ops1,9f598cdb10cb2b680557a0ee018a7ccc697bda02.
Recipient: isolated codex/dev-skill4-skills; source checkout remains read-only.

Recovered S-2 from the original ACCEPT-WAVE3 local HTML artifact, lines327–336,
not inferred from the supplied summary. Artifact SHA-256: `76d63ebb1ead7c1e8d42c0d64ac1397e9d06df9b695f43ae8987b8c5529c74cf`.
Original report title: “The five new skills are invisible to Claude Code.”
Observed: `.agents/skills` has six directories, `.claude/skills` has only
`civic-prose`; the existing 77 checks read only the first root. Required repair:
mirror five SKILL.md entrypoints and check equal discovery sets. The new control
reproduced one failure/77passes before copying any mirror.

Preserve concise factual orientation under the explicit 2026-09-09 owner
authority; S-1’s proposed blanket-rule reversion is not current authorization.
Verify actual installed discovery/config, mirrors, reusable American-English
and contextual role checks, with no global settings or permission changes.
One bounded read-only helper inspects existing en-US gaps; parent implements.
Keep independent acceptance separate; no self-merge or monitor.

## Fresh adoption and completed focused evidence

Installed Codex 0.153.4 `debug prompt-input` discovered civic-prose and all five
workflow skills from this recipient's `.agents/skills`. A fresh strict-config
app-server `config/read` reported a helper ceiling of 2, no helper model/effort
defaults, and unchanged on-request/workspace-write settings. No config was
written by either probe. No-recursion, zero-helper planning, read-only default
and parent integration remain instructions, not falsely advertised runtime
restrictions. Existing sessions do not retroactively adopt project config.

The exact S-2 control failed one of 78 tests before mirrors; the corrected
inventory passed. Expanded portability now covers the read-only terminology
reviewer and preserves complete provider-adapted bodies. American-English
checks reuse the existing word rules and cover explicit authored data fields;
exceptions require source/preservation reasons. Grounding remains independent.
LEG owns actual legislative copy repairs; this branch changes no LEG runtime
content. Approved concise canonical orientation remains intact.

LEARN: test both discovery inventories and complete mirrors, not only the
primary provider's skill root. Keep the shared authored-copy checker distinct
from semantic role review and from immutable source/save tokens.

## Completion

Implementation and local validation completed on executable head
`7761a3a47e1a47a02e490c5cb9d4ea82f3d95db5`. The subsequent plan-closeout
commit changes documentation only. Full `npm run validate` passed: 178 test
files, 3,246 passing tests and two existing skips, followed by source validation,
source replay, production build, deterministic demo and art validation. The
explicit inventory and QA art commands also passed. Focused checks total 97
with the reasoned authored administrative-queue usage control.

Draft PR147 remains unmerged. Independent acceptance and composition with
other owners remain review gates, not author self-acceptance. Exact-head CI
was observed in progress once; no monitor or global configuration change was
created. The heavy slot is handed to LEG after the separate PR138 sequence.
