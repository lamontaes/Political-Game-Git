# PEOPLE-WEB17 — relationship web and one person card

Owning packet: Drive PEOPLE-WEB17. Branch from UI-bearing `codex/ui-core-release-transfer` at `a5825fef`. PT3 keeps PlayerGame, the shell reducer, conversation, and global shell CSS. This plan owns the graph read-model, the web drawing, the unified card, and a small mount adapter.

## Done when

- People opens as a relationship web with list/category alternatives.
- Room inspect, People selection, and pins use the same person card.
- Edges are known/public/reported records, not inferred friendship.
- Existing pins, portraits, conversation, and saves stay valid.
- PT3 receives `src/player/people-web-adapter.ts` as the mount seam.
