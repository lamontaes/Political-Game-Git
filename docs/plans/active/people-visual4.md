# PEOPLE-VISUAL4 — real modular people continuation

Current authorization: FINISH-WAVE4 shared contract and section B (2026-09-09).
Resume existing #134 / codex/people1-r1-transfer at
44aea4bf891290055b490e845b5387a4d93f4e32; fetched main is
6b95f3713590f18973e5e54a3da86e6cd66aa734. Clean owned isolated worktree,
preflight passed and no active heavy runner found before writing.

## Implementation sequence

1. Fit review cameras to all rendered layer extents, preserving body/garment
   geometry and exposing head-to-feet evidence. Reproduce prior clipping.
2. Append independently authored candidate attachment/reference revisions from
   visible source pixels. Do not rewrite rejected D-068 anchors or claim hidden
   joints were measured. Preserve source hashes, uncertainty and rights.
3. Establish one diagnostic exact-pair reference frame using the accepted
   ease-preserving metric and unchanged 3%/transform bounds. Apply the same path
   to every supported existing body/garment/head/hair/footwear pairing; enumerate
   excluded exact pairs and missing pixels, without declaring metadata absence
   to be art absence.
4. Recover native masters through existing lineage while engineering continues.
   No image generation, native-detail claim for an upscale, or self-approval.
5. Carry compatibility-driven selected saved people and wardrobe A/B/A through
   scene, dossier, conversation, creator/hero and reload via one shared adapter.
   #144 owner applies the shared root/navigation patch and proves its caller.
6. Publish corrected checkpoints promptly on the same unmerged PR. Run scoped
   failing/passing controls, required art commands, coordinated full/browser gates,
   inspect screenshots, record exact head and finite UI/landing handoff.

## Authority reconciliation

The prior transfer's human-coordinate-authoring dependency is superseded by B's
explicit permission for game-authored candidate coordinates and overlays.
D-068's old records remain unchanged/non-authoritative. New authoring is labeled
as game-space authoring, not measured invisible anatomy. Production promotion,
rights and visual acceptance remain separate gates. Framing changes the review
camera only; it does not improve a fit metric or stretch a person.

Two bounded read-only helpers trace native source masters and the accepted fit
reference-frame contract. No recursive helpers or duplicate writer.

## Corrected checkpoint

The implementation now carries206 exact supported wardrobes across5 standing
bodies through the shared render plan; partial identities remain selectable.
All9 head targets expose55 existing frontal hair styles plus bald. The source
and failure matrix is in `art/qa/people-visual4/README.md`; the generator records
all532 body/wardrobe cases instead of hiding failed fits. No production release,
source admission, rights inference, generation or enlarged raster was added.

The two bounded helpers completed exact-row metric/selection/scene-adapter work
and source/hair attachment recovery in isolated worktrees. Their source hashes
and commits were retained; no paused source workspace was written.

#144 requested and receives `PersonAppearanceControls`, shared portrait options,
and the per-person saved wardrobe map in `planLifeScenePeople`. UI-owned root,
persistence and navigation files remain with that owner. Its combined consumer
mounting and normal-route browser proof are explicitly separate evidence.

## Architecture integrity and LEARN

- Simulation remains pure TypeScript: the only canonical extension is an
  optional explicit appearance selection. Legacy recipe bytes are a regression
  control; seed, recipe version, catalog pin, established facts/history and
  serialization are preserved. Wardrobe remains per-save interface state.
- No Stage6 semantics, law/election engine, environment authority or World
  identity was changed. All new source coordinate records say game-authored;
  missing palette/pose pixels and unknown rights/lineage remain explicit.
- Art release, exact-pair compatibility, measured fit and visual acceptance are
  separate gates. Candidate lifting is isolated; the production fixture guard
  still refuses regression art as a human likeness. D-080's bounded warp gate
  remains intact.
- Recurring lesson encoded in code: maximum proportional error cannot follow
  only the maximum-pixel row. A narrow-row regression prevents that false pass.
  Foot coverage now checks internal holes, and reports shell ratios separately.
- Recurring lesson encoded in tooling: `--refresh-measurements` recomputes old
  evidence while checking retained source/derivative/registry bytes. It avoids
  either leaving stale metrics or regenerating historical enlarged candidates.
- Recurring lesson encoded in the UI adapter: dependent identity filters avoid
  full cartesian enumeration without capping choices; invalid saved wardrobes
  stay visible and refuse rendering instead of silently changing identity.

Focused metric, identity/wardrobe, hair/head, scene-adapter and framing checks
passed. Art validation/inventory/QA ran (1868 inventory items; duplicate-source
hash diagnostics retained). Full validation/build and browser proof run only in
the coordinated PEOPLE slot after ENV. Do not treat this checkpoint as that
pending evidence or as human acceptance.
