# Systemic modular repair

Single worker; no delegation. Owner-confirmed Astra High/Standard and $20 cap.
Baseline: local MODULAR successor 7618994adf5741143b2e91ca152d2b420baf6daa,
including recovered a9755868604694ee3b9f3082640ab97cf97283ac. Refetched main:
fed321f7667bbe5c3570679554a2f6b88d3bf8b1. Isolated worktree:
/private/tmp/pg-systemic-modular-repair, codex/systemic-modular-repair.

Ownership request and stopped-writer continuation recorded in the existing
MODULAR Drive handoff. Retained session 999303b4-f0e4-4b0d-963c-8e3a33404775
records wrap-up, owner interruption, and stopped background work. Its source
workspace, LAND, owner Play and saves remain read-only. No fresh peer ACK claimed.

1. Reuse supplied 27-test fit/material bundle and actual private generation-13
   prep, preserving source hashes and all previous generations.
2. Add versioned data-only semantic calibration, uniform head/socket solver,
   grouped hair/mask transforms, readable refusals and immutable cache keys.
3. Repair raster materials through explicit protected masks; keep original alpha.
   Keep body-owned neck/collar layers and logical hair selection stable.
4. Register additive private candidate generation, verify 18 fits/54 hair cases,
   all seven skin ramps and held-out real source + synthetic new socket.
5. Exercise normal appearance/room/portrait/save route where available; report
   PASS/FAIL/UNRUN separately. Candidate engineering never grants art approval.
6. Run focused tests, type/lint and validate:art/inventory:art/qa:art; package
   source and compatible private inputs for existing MODULAR/LAND receiver.

Initial receipt: supplied Python tests 27/27 PASS. Usage meter at start: ordinary
usage allowed, balance 500 credits; dollar conversion unavailable. User confirms
settings and enforced cap. No generation, purchase, alternate API or subagent.

Implementation checkpoint (2026-09-17): generic descriptor fitting, registration,
immutable generation freezing, skin/hair raster remap, logical identity retention,
semantic portrait framing, and the same fitter for explicit pose sockets are in
place. The private regression bank covers 18 head fits, 54 hairstyle fits and 288
pose combinations. An additional real retained prepared head and explicitly
synthetic body socket register through data and pass the existing outfit resolver.
No newly painted head/body is claimed by that rehearsal.

Retained authoring files engine-people40/check-playable.ts and
engine-people41/check-playable.ts cause 39 pre-existing no-explicit-any errors in
whole-tree lint after the private pack is staged. Changed-source lint is checked
separately; the private files are not silently rewritten or excluded from that
reported result. Candidate pixel acceptance is separate from engineering tests.

Final bank: generation 14 revision `s14d`; 84 standing parts and 396 auxiliary
pose layers / 288 pose fits. Rejected derivative iterations live only in the
local diagnostic archive, outside active asset globs. All 480 actual standing
material/part combinations preserve alpha and excluded RGB exactly. Retained
input hashing finds only the intentionally additive MODULAR registry changed.
The private pack installer preflights source hashes and every pack file before
copying missing files; it refuses to overwrite any different existing file.
