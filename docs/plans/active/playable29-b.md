# PLAYABLE29 B — valid outfits, then prepared materials

Owner authorization: PLAYABLE29 P29-B, option A, 2026-09-13. B owns appearance runtime/controls; A owns shared root; V owns art/authoring/engine-people29. Current baseline2d154752; old Bcad73430 is banked, originals and generations preserved.

First deliver pure complete-outfit validation/search, supported controls and one canonical validated appearance/outfit write before autosave. Fresh candidate initialization only may select complete identities; established people never reroll. Old invalid records require explicit recovery. Frozen root adapter to A, then publish safety increment.

Then consume V's versioned prepared SVG materials/features through the existing image-layer path. Explicit tone ramps/masks preserve ink/shading/alpha; bounded complete feature groups preserve neck/hair constraints. Cache on demand with cleanup, old unmarked rendering exact. No new renderer, paid generation, training, source replacement or art acceptance.

P29 explicitly authorizes persisting actual selected recipe and outfit. This extends D-054's historical derived-only wording only for newly initialized or explicitly edited appearance records; old records remain byte-stable. World remains pure data, renderer read-only, canonical autosave remains authoritative. Shell wardrobe maps remain readable legacy data; a new atomic appearance outfit takes precedence over its stale shell counterpart.
