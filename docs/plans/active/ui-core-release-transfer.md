# UI-CORE-RELEASE transfer

Exclusive recipient: Codex in `/private/tmp/pg-ui-core-release-transfer`, branch `codex/ui-core-release-transfer`.

## Recovery

Source workspaces remain read-only. Claude UI tasks were visibly Error/stopped; no matching UI process working directory was returned by lsof. Hash manifest records both stable dirty checkpoints. Production commits d12a270 and 455b87b are retained as ancestry and reconciled with accepted main 1eb0b0d. Eleven unpublished browser-support edits were replayed. Prototype #133 remains untouched at ae9ed995.

The separate person patch implements an older overlay for the same selected-person defect. Its full patch, untracked components/tests/screens remain at `/private/tmp/ui-core-transfer-recovery/person`; reconcile its distinct evidence against the recovered shell's existing ID-preserving dossier and eligible-addressee routing, not two simultaneous person surfaces. Physical presence alone must not substitute for eligible addressee authority.

## Delivery work

- Retain creator draft/answers across questionnaire Back and expose existing deep questionnaire.
- Verify selected person A/B/A, supported Talk, mixed pin persistence/migration and pure menu reads.
- Port corrected prototype layout primitives without importing prototype data.
- Integrate frozen feature adapters from existing owners; preserve canonical World/person/time/save authority.
- Run focused regressions, final integration/art gates, actual desktop pointer/keyboard and visual proof; publish one production PR, unmerged.

## Shared adapter seam

UI owns `src/App.tsx`, `src/player/PlayerGame.tsx`, shared shell/navigation/CSS and creator transition wiring. Feature owners deliver immutable World input, canonical playerPersonId and `onWorldChange(nextWorld)` output; navigation/entity requests use explicit canonical IDs. Components must not instantiate a replacement World during render. Provide exact commit and feature-local files plus a proposed root patch against this recipient for review. Root patches remain UI-owned.

OPENING's `OpeningLifeFlow` / `OpeningLifePanel` and controller seam received; PEOPLE and ENV supply existing deterministic person/scene adapters with released-art gating. MUNI provides feature-local canonical public-work panel. DEV/JUD/EXEC/LIFE requests posted successfully via gh to PRs #138–141; connector comments failed 403. Native outgoing task tools unavailable here. No heavy gates running yet.

## Acceptance

Recovery checkpoint only; implementation/integration and current-head proof pending. Prior owner's tests are historical evidence, not recipient acceptance. No self-merge or release activation.

## Integrated checkpoint — September 8

Production route remains `/`; `?view=review` is DEV-only. The integrated creator keeps draft/priors on Back and exposes deep calibration. Canonical selected IDs flow through rail, quick/full dossier and eligible Talk. Pins/preferences are written in the existing IndexedDB interface store before Save reports completion; a slow save cannot replace newer in-memory World state.

Frozen dependencies: DEV0a8a56a, LIFE5d142e7, EXEC5b9076a, JUDc0baaf5, ENV97fbcde (plus VenueActivityPanel receipt from544d78b), PEOPLE production adapter paths fromd9d3afe. JUDb7417ab has no additional production-code delta; its later evidence/corpus snapshot is not a combined-tree count baseline. No candidate art was released. PEOPLE's candidate/review cargo stays with its owner.

Normal Day registers LIFE and ENV; Work registers JUD and authority-gated EXEC beside the recovered legislative route. The shared existing time registry composes supplied LIFE/EXEC handlers; root action callbacks synchronize the existing executive inbox. Executive ordinary entry still depends on its source/qualification authority. No new selection engine was added.

ENV actual attendance now governs immediate shell place/presence. Household sprites, narration and earlier-scene conversations are withheld in the completed activity aftermath. The receipt and backdrop survive save/reload at the same canonical instant. Later time does not imply persistent attendance.

OPENING/MUNI/recovered LEG frozen deliveries remain pending. Existing owners were contacted through PR133/137/138–142 comments because native outgoing task tools are absent. No duplicate owner or root writer was created.

## Evidence and limits

See `docs/plans/evidence/ui-core-transfer/VERIFICATION.md`. Current focused proof is 47 tests across five files; all four normal adapter browser tests pass, with an additional strengthened ENV backdrop/reload test passing. Art validation, inventory and QA generation pass. Full exact-head suite and human visual acceptance are still pending; the serial heavy slot was requested on PR90 and133. The initial full command stopped at formatting, now corrected.

## Architecture disposition and LEARN

- Confirmed compatible: one canonical World/person/time/save authority, pure TypeScript simulation, read-only shell navigation, stable references, additive interface-store migration, released-only portrait selection, existing feature-local writers and canonical handler composition.
- Corrected now: creator draft reset/deep-path omission, pin Save completion ordering, stale focus state, rail control overlap, unsupported public-biography attribution, and household claims after actual venue attendance.
- Dependency-bound: OPENING transition/scene agency, MUNI public-work panel, recovered LEG multi-document adapter, ordinary EXEC authority prerequisites. Unsupported scene/portrait assets remain explicit absences.
- Rejected: duplicate old person overlay and prototype identities/clock in normal play. Both stopped source checkpoints are retained as evidence instead of installing competing surfaces.

LEARN is encoded in executable proof: pin tests wait for actual Save completion and compare all reference kinds/order/sizes; LIFE scheduling tests assert the real conflict, perform the existing activity, then verify the exact new session. A generic calendar row did not prove the study-session integration. Source identity includes generated tracked art reports: running art generators during a browser proof invalidates its fingerprint, so all tracked generators must finish before the browser server starts.

Historical recovery follow-up received from OLD-WORK-RECOVERY1: raster decode errors and placeholder rendering need the existing visual owner's review. They are not silently marked repaired by these shell tests.

## Later integration checkpoint

LEG owner published80801a9 on PR137; integrated via8959726. Docket uses the existing Work frame, callback and save store. The currently selected document can be pinned by its canonical measure ID. Two root/browser checks pass, including selected-document reload/pin persistence. UI's transition fade now precedes generation and has reduced-motion support plus a once-only completion guard; creator09 passes named-draft recovery and keyboard Begin. OPENING will connect its frozen controller at this same root-owned boundary.

Source validation, source replay, production build and headless demo pass. Full suite remains queued behind PEOPLE/ENV. Current evidence ledger records exact runs and incomplete acceptance.
