# MUNI-PLAY1 feature-local handoff

Existing owner: Codex task `01a0837d-a9a8-73b2-906d-11477f179bf8`. Root navigation remains UI-core's. No deployment or automatic merge is authorized.

Mount `MunicipalWorkspace` from `src/player/MunicipalWorkspace.tsx` with the canonical `{world, onWorldChange}` save path. Pass the composed `transitionHandlers` registry so attending an activity runs every installed clock transition owner. Inspection does not mutate residence, government membership, or the world.

The component reads the saved person's home place; only a verified government-unit/place binding supplies the home government. The library selector exposes all compiled governments independently of home or office. The explicit public-session button creates an **authored game occurrence**, with disclosed timing/duration, on the existing calendar. It does not assert a real published notice, agenda, convener role, or legal office. Public attendance completes the canonical activity, consumes time and records history. Meeting-notes work requires a current participation in this exact municipal organization. Qualification/candidate rules remain QUAL-COMPLIANCE1's.

For ENV, pass `renderVenue(world, activityId, venue)`. The supplied venue comes from `municipalVenueForActivity` and identifies the source-supported compatible Community Center room context. Call ENV's frozen `resolveActivityVenueScene(world, personId, activityId, venue)` before rendering with the existing compositor. Only that resolver may approve released art after completed attendance at the current instant. An invitation, library inspection, a later clock instant, or a travel record must not paint presence. Generic civic art is not an exact reconstruction, and baked spectators are not canonical attendees. Other rooms stay unresolved.

`tests/browser/municipal.html` is an isolated verification entrypoint with explicit authored-role fixtures, not a player navigation alternative. Run the browser proof with a dedicated identified Vite port; do not reuse another task's server. Normal-route integration and human visual acceptance must be recorded by UI-core separately from this feature proof.
