# NEXT24 A — frozen desktop source receiver

Preserve accepted main `215b3e90d163ed3344dbbd36bfaa621501bd67f8`
and desktop owner source `d5bbc5e0a60fc9a6730b4651c4b13bc40a31ed82`
with a source-preserving merge in this isolated receiver. Do not mutate the
root, frozen play trees, desktop owner tree or running installed update proof.

Runtime auto-merges; regenerate the seven conflicting derived prose reports
from the combined sources using the existing corpus tool. Preserve monotonic
anchor issuance, burned IDs and existing computed surface registrations.
Verify portable interface versions 1/2/3 and future-schema refusal, desktop
deterministic tests, current PlayerGame composition, required art gates and
build identity. Record exact commands and their results before publication.

Installed acceptance is separate: artifact run 34720690158 is still active;
internal-art-review job 103625859749 failed after the offline pass with
`Page.handleJavaScriptDialog: No dialog is showing`. No artifact from an older
head is relabelled as this combined source. Human visual acceptance remains
pending. E PR #220 and final CIVIL PR #223 remain separate frozen follow-ons.

Preflight: clean receiver at main; first checkout failed ENOSPC and Git removed
its incomplete tree. Retry succeeded after free space increased, without
deleting or cleaning any existing owner or play workspace.

LEARN: refresh actual listener identity with permitted loopback access before
assuming a sandbox connection refusal means the existing server stopped.

## Receiver verification

Combined-source checks passed: 76 tests across shell state, World repository,
art preview and build profile; 34 Node desktop tests; 42 anchor-history tests;
corpus check (2,644 templates, 265 warnings, seven byte-identical generated
artifacts; HTML differs only in recorded revision); lint; typecheck; all three
art gates (1,872 inventory items, existing duplicate-hash warnings retained).
The anchor sidecars are byte-identical to the frozen desktop owner source.
Production build is in progress; no installed proof is claimed for this merge.

Published follow-on receipts received, not replayed or yet banked here:
D PR #221 `b219a4868f363a3b2784b0910948c567e3962598`, CIVIL PR #223
`151422197495a0e61ace9c4d6480d0cd9a5772da`, Budget PR #222
`72c48b59ee9554ed4c800d3c9b7df51e9f263620`. Each requires its documented
minimal current-root adapter and a regenerated combined corpus, not whole-root
replacement. D's previous destination refusal is superseded by its verified
owner publication receipt. New People/B and G acceptance remain excluded.

E PR #220 at `88c3080d` has a passed PR repository job but a failed push job:
the docs-only range `e69bcd40..88c3080d` omitted a newly changed declaration.
Broad browser jobs also failed; these are not called green or classified as
mere load. Core #219 broad browser aggregate likewise failed despite two
passed repository jobs and passed targeted proofs. The existing clean core
play server remains source `c0b0ba33`, digest `de6bcbc1dfff717adb006880cbfe418b2cd2841407e568bde57fb05f9eddefd0`,
PID 21217 on 5198; title verified visible again without restarting it.

The attempted scoped desktop-owner notification was rejected by auto-review
because destination authorization was not established. Nothing was sent; no
alternate communication route was used.
