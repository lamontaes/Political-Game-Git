# NEXT24 A — frozen desktop source receiver

Preserve accepted main `215b3e90d163ed3344dbbd36bfaa621501bd67f8`
and desktop owner source `d5bbc5e0a60fc9a6730b4651c4b13bc40a31ed82`
with a source-preserving merge in this isolated receiver. Do not mutate the
root, frozen play trees, desktop owner tree or running installed update proof.

Runtime auto-merges; regenerate the seven conflicting derived prose reports
from the combined sources using the existing corpus tool. Preserve monotonic
anchor issuance, burned IDs and existing computed surface registrations.
Verify portable interface versions 1/2/3 and future-schema refusal, desktop
deterministic tests, current PlayerGame composition, required art gates and
build identity. Record exact commands and their results before publication.

Installed acceptance is separate: artifact run 34720690158 is still active;
internal-art-review job 103625859749 failed after the offline pass with
`Page.handleJavaScriptDialog: No dialog is showing`. No artifact from an older
head is relabelled as this combined source. Human visual acceptance remains
pending. E PR #220 and final CIVIL PR #223 remain separate frozen follow-ons.

Preflight: clean receiver at main; first checkout failed ENOSPC and Git removed
its incomplete tree. Retry succeeded after free space increased, without
deleting or cleaning any existing owner or play workspace.

LEARN: refresh actual listener identity with permitted loopback access before
assuming a sandbox connection refusal means the existing server stopped.
