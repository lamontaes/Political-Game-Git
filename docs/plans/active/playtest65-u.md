# PLAYTEST65 receiving register

Current final receiver: O, Codex task `01a0bc97-c12c-79e2-963a-2d70ed8e4599`.
Console/UI engineer and current PG-LAND writer: U, task `01a0bb17-2e65-78b2-be17-12879579157c`.
Current routing supersedes the historical U receiving duties below; past receipts remain evidence at their stated sources.
Workspace: `/Users/lamontae/Documents/PG-LAND`; branch `codex/playtest65-u`.
Assignment: CRUNCH47 PLAYTEST65 CODEX DELIVERY, common/interface/U/shared completion.

## Common source

`1cdbe0c493624e837b8bca79d9d793f866fe216d` merges played modular
`65f7704b6f35ba8e2a418d4edb74dfdbbf26433f` with completed storage guard
`46b0f9dab40bb3829d444f8abc1e5f40233c3180`. Clean automatic merge; original
branches preserved. Origin is the existing public lamontaes/Political-Game-Git
repository. Public push awaits explicit destination/payload approval after
automatic approval review rejection; local peer receiving is available.

## Ownership and receipts

| Lead | Task                                 | Workspace    | Owned surfaces                                                                                                                                                   | Receipt                                           |
| ---- | ------------------------------------ | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------- |
| U    | 01a0bb17-2e65-78b2-be17-12879579157c | PG-LAND      | PlayerGame, root mounts, shared controls/styles/window/navigation, creator/opening/map/Calendar/person/News UI and native console; frozen changes delivered to O | common source COMPOSED                            |
| V    | 01a0af7c-3a97-74b2-adcf-407cb5182b4b | PG-MODULAR-E | modular source, geometry/material/pose prep, appearance resolution, renderer/cache, reusable figures/portraits, Firefly kit/private pack                         | entry ACK; common source ACK                      |
| W    | 01a0bb17-acba-7740-89f6-7892785fa315 | PG-PROSE     | world/people/history/opening/birthday/travel/education/opportunity domain adapters and writers                                                                   | entry ACK; common source RECEIVED via local fetch |

Direct supported task messages are the active peer channel. This is the single
PLAYTEST65 receiving register, with O owning final integration/delivery; do not wake historical Claude owners. Use the existing U/V/W tasks, no subagents.
One heavy Mac operation at a time, allocated by O. Existing storage CLI
and registry apply; no historical deletion. Protected packaged 65f7704b control,
private immutable pack, recovery and both owner save profiles remain intact.

## Interfaces

- W: existing projectOpeningLife/projectWorldOrientation and canonical writer
  path; prepared opening beats, legitimate shared person record, activity and
  opportunity projections. Birthday completion returns a complete setup before
  Next; existing setup state owns it. No second world or save store.
- V: supported logical choices and fit reasons, safe preview/current recipe,
  pending/ready/latest request identity; saved figure/portrait and real pose/scene
  anchors. U owns selector JSX and shared layout subject to explicit leaf ACK.
- U: typed mounts use the real readers and writers; shared registries and release
  declarations compose once after frozen peer returns.

## Completion checklist

- [ ] Creator arrows/swatches/immediate safe preview and unclipped actions
- [ ] White House, Congress, state/DC, locality, life, physical starting scene
- [ ] Map current/home framing, labels, legend, unified selection and records
- [ ] Reusable movable/resizable viewport-safe windows and saved layout
- [ ] Radial close/submenu motion, keyboard/reduced-motion/focus/save/quit
- [ ] Calendar, shared person record, Personal, News, opportunities and travel
- [ ] Discreet actual source identity, detailed About
- [ ] Frozen V/W deltas received, focused checks and disposable combined journey
- [ ] One private packaged successor launched with exact source/pack/profile

Human artwork acceptance, source reception, public/main release, package creation
and loaded private candidate remain separate states.

## First receiving checkpoint

W commit `6273e9fc` RECEIVED as `6dec4720` (16 domain files). U first real
TypeScript check passed, including test import resolution. Focused map/saved
shell/opening checks passed; radial spacing test caught a too-tight outer ring.
The repaired radial and new saved-window/map tests then passed 28/28. No
assertions removed; positional expectation updated for the intended smaller fan.
Changed-source ESLint passed after type-import cleanup. Browser and packaging
remain pending; these checks do not establish visual acceptance.

V early Firefly kit SENT in existing private exchange folder
`110W6Ta8MCg8gTQLcrneHKhYyvj3zJsRJ`, 39 files / 18.5 MB reported by V.
White House candidate artwork and renderer delta are not yet received.

## Second receiving checkpoint

W `3c894cb0` RECEIVED as `57be7433`: person history/contact, opportunity/activity readers, initial home placement and new-opening replay fields. W reports 24 focused checks and scoped lint pass. V has released the shared Mac slot after typecheck and 10 focused tests (one private-input skip). U now owns private-input installation and the first combined browser window.

U wired shared Contact, walk/Attend guards, Save and Quit, Personal history/goals, compact work/study, map state labels, and removed office browsing's duplicate day control. Changed-source lint/format passed; combined typecheck/browser still pending. Q65-W-002 matching public-meeting return journey is awaiting the owner's explicit gameplay decision; local neighborhood/home remains supported.

## Art Bench first — September 19 owner priority

Read ART BENCH FIRST through END ART BENCH FIRST. U owns ArtDeskView, native
client/preload/export, intake/Drive bridge repairs and the working round trip.
V owns canonical candidate ingestion and reusable people/clothing/pose source.
Background generation is paused until the owner can use the requested workflow.
W continues the bounded Michigan municipal/party/demographic domain delta;
politics-government.ts is W-owned, GovernmentBrowser rendering stays U-owned.
Existing supported task messages carry acknowledgments; no new receiver.

Preserved combined source `27c4c10363684d105f1d0dbdf30691352451ba7a` includes
V saved figures/layout and environment registry (`1041f355`, `8bb93c58`),
W final source `48a03476`, U title/opening and Creator Undo. Combined typecheck
passed. Browser caught a real creator defect: the figure shrinks to 85px at
1280×720. That fix and remaining game journey/package are pending while bench
work takes priority; no visual acceptance claimed.

V received 25 images into the installed store
`~/Library/Application Support/Our Civic Duty Private/art-records/ocd`, store
`store-b40dfbc0-bcee-4749-b570-1c9db4591e1a`. Existing 103 events and review actors
were preserved. Six original Firefly images, two edited environments, ten
people/clothing source candidates and seven actual references/guides are
ingested. Receipt: `PG-MODULAR-PACKS/PLAYTEST65-FIREFLY/receipts/artbench-ingestion.json`.
White House original `cand-18627397-9b4c-eed8-434c-c307c1b0b835` has wide child
`cand-bf01adf1-0c1f-10e6-7fd0-cfe659bd3a7a`; desk original
`cand-f331bb9d-b761-a398-6f38-d0183caa0495` has child
`cand-51191810-44c4-326a-fb42-4808188a9386`. Civic duplicate aliases existing
`cand-e49e253a-bc98-09b6-532f-0e16d7d97f4f`. No owner art approvals were added.
V released the sole store writer window after restart and duplicate-sync checks.

U implemented exact candidate/revision/hash native export preparation and drag,
readable revision export wording, explicit child reimport drop zone, provider
prompts/count/copy, visible lineage, remembered viewed candidate, unapproved
verified catalog bytes, and local received branch selection in the same client.
Native safety/download tests passed 5/5; focused intake/bridge/card tests passed
81/81. First typecheck passed. Packaged client loop, native payload, real Drive
readback and final source checks are still pending at this checkpoint.

## Bench round trip and permanent console checkpoint

Packaged host `8eb282d2` with bench source `d71f2e2a` passed the real existing-store
round trip. Evidence is `test-results/runs/playtest65-u-bench-loop/result.json`
and desk/original/revision/qa-child captures. Original and revised White House
and desk images were visible; exact wide PNG download matched SHA-256; external
sips crop reimport retained request, asset, parent, tags and calibration flags;
restart/reload kept the selected revision and all prior review events unchanged.
Native export preparation verifies candidate/revision/hash and immutable bytes;
external browser drop remains unobserved and is not claimed as passed.

QA child `cand-317b8d28-e494-48b9-b1fc-d803c89a568b` has SHA-256
`f4eec6689bec4f5aa33ce9c2b7e830064e9d9199fd81c59aef6105cfc7a716bb`.
V independently downloaded those exact cloud bytes (2,329,499 bytes) and verified
parent/revision metadata. Its explicit QA disposition is being appended, without
an owner approval. All 17 prepared source/material-map candidates arrived; their
explicit child role tags need the supplied corrective events. Future child
intake now appends attributed tag overrides while keeping inherited tags intact;
56 focused store/duplicate tests pass. Prepared head/material remains unapproved
because V found a skin-ramp defect and is repairing it.

Real installed startup exposed stale Art Desk worktree metadata. The current
source identity probe also formerly exceeded its health deadline; content-hash
caching now retains per-file stat/ctime checks and reduces repeated probes from
3.3s to 0.26s. Identity/privacy harness checks passed, including same-length
rewrites with restored modification time. No readiness timeout was relaxed.

The permanent-console delta was read. Actual Dock target is the r1 staged app;
both it and /Applications/Our Civic Duty Private.app use bundle identity
com.ourcivicduty.private-controller. The r1 output is registered as protected.
Owner explicitly authorized the controlled quit, stable installation and single
Dock retarget. Preserve prior installation and all canonical state for rollback;
verify the actual loaded application after installation.

W frozen increments afdb35d3, 2b2a522e, 5140ea3b, 1e9bb51a are acknowledged and
queued after this bench endpoint. W reports focused 12 government plus 14
contact/acquisition tests and canonical typecheck pass; U composition remains
pending. Broad municipal/ACS source coverage remains incomplete, not zero.

## Permanent installation and received-source checkpoint

Owner-authorized installation completed at `/Applications/Our Civic Duty Private.app`.
The loaded native host is d10739cf, same bundle ID, ad-hoc signed arm64. Deep strict
code-sign verification passed. The old Applications installation, r1 pinned copy,
settings and state backups are retained. The single Dock target now points to
Applications and every unrelated Dock item is preserved. Actual installed-path
launch and exact revision PNG download passed; a Dock-click launch itself was
not observed. Receipts are `playtest65-u-bench-loop/installation.json` and
`dock-continuity.json` under test-results/runs. Native host update/restart was
explicitly authorized by the owner; no public release occurred.

Art Desk local source/pin was set to d10739cf to avoid the broken historical
worktree. The running source now includes efaafb2b: a QA child no longer hides a
normal card; revision strips label QA only and arrival notices skip QA children.
17 card tests passed. The final clean received HEAD must replace that pin before
the next controlled restart. QA child disposition and 17 explicit prepared-role
corrections synchronized; owner review decisions remain unchanged. V verified all
49 input IDs, hashes, parents and tags against the cloud catalog, including r6
face repairs and four coily-hair records. Frozen r7 is candidate source only:
7,505,784 bytes, SHA a60d1bd5c2bae952c0054f4b82ddac15d46260122360224abb0a7c1b79040375.
V replay passed 113 files and immutable originals; this is not runtime admission.

External-browser drag remains unverified: native pointer input reported
noWindowsAvailable on one display, and the client changed state before another
drop could be attempted. Neither U nor V claims an upload or generation occurred.
The completed local exact-file external crop/reimport/restart/Drive round trip
and this named external boundary remain separately recorded.

W commits received: afdb35d3→421a6c08, 2b2a522e→7c190038,
5140ea3b→8ee2afc8, 1e9bb51a→3757a946, 1103f03e→2a4143f1,
07de4d6e→7922e5e1. These add neutral local institutions, public chapter contact,
bounded source acquisition, retained mayor identity, Portland charter evidence,
and selected-area demographic reads. W's final adapter tests passed 11/11 plus
scoped lint. U owns its map mount and composed validation. Current source manifest
was regenerated. The short creator check still failed at 180.9px stage height at
1280×720; U corrected the actual Advanced margin without weakening the assertion.
Combined gameplay acceptance and final prepared private payload remain pending.

## Owner queue clarity, questions and GitHub update repair

V source received: 8d5e8a02 → be5a1cce (prepared renderer/material transitions,
rig contact data, immutable external-return intake); 545e87ad → b1e1c43b
(owner-approved White House revision 6). The exact private White House file is
5504×3072, SHA d160ee6a08e708b9f0fef68b1a0488c81fad23d426fa548adc59364804808835.
Coordinates remain normalized against the authored layout canvas. This is an
external enlarged derivative, not a native-4K claim.

Combined creator/opening/person/map/Guide/News/movable Calendar/Save-Continue
browser journey passed at b1e1c43b plus the recorded dirty-source digest in
playtest65-u-review-browser3. Creator checks covered 1280×720, 1280×860 and
1024×768. The separate Art Desk action journey passed in review-browser4 and
again with saved questions in friendly-browser: latest thumbnails, failed-write
retention, approve→Approved, reject→Rejected, reference/archive/restore, briefs,
exact downloads, and question persistence. These are automated and visual
inspection receipts, not new owner art approvals. Calendar attendance exposed
remote-work routing and opening Skip overlap; repairs require the next run.

The owner requested only essential asks, plain language and replies to notes.
V reviewed 144 requests/208 candidates without constructing another store writer.
U applied 215 attributed organization-tag events and two V replies through the
running canonical API. All existing decision event IDs were compared unchanged;
receipt: playtest65-u-bench-loop/essential-review-organization.json, lastSeq578.
Candidate-bound organization tags never hide a fresh revision. Old no-image
requests are recoverable in Library; new images reappear for review. Questions
and replies are append-only message.posted events, linked to the exact prior
question or review note. Five-minute V heartbeat answer-art-desk-questions checks
new notes and stays quiet when unchanged. It cannot claim an answer when the
console/API is unavailable. A second canonical writer remains prohibited.

A prior stale-store append produced historical sequence collisions (348 event
IDs, maxseq344). IDs and owner decisions were preserved without renumbering.
The live store now reads newly appended IDs before projection/append and rejects
stale tag versions. This is not a general simultaneous-multiwriter lock. Focused
store/card/Calendar units passed61 before the last small UI repairs.

Owner newly approved Desk revision4 (cand-1f3bd030-77c1-428b-bea3-2019f193c6aa,
SHA5579b2fae3ee083979ea75e1468eb873cb107322abe7413269df3bb67bd55e5a) and civic
revision3 (cand-48b10062-b064-46bf-8194-500b0d66cc83,
SHA1bd1af90f54e7a4764e543e2a503008ad3b2a6ce1b315debcfd0797045c338a1).
Both are5504×3072; V verified and visually inspected their exact bytes. Neither
currently has a live scene consumer; keep Approved / waiting to be implemented.
Do not invent a parent for the civic upload, recorded as an original.

GitHub main checked live:7869561ee8b273796bebe82c803e9d477dd6a907. The installed
main remainedfed321f because the updater wrongly paired main with gen16's newer
source contract. A separately named main package was assembled from1693 exact
referenced legacy files (245,179,898bytes),43manifest seeds,0unresolved paths,
no newer modular lineage;700main consumer/manifest hashes bind its source.
Original gen16 is unchanged. New package main-7869561e-retained-art-06b10d898a07,
manifestSHA06b10d898a07ab335969cf3e34472b664bd8ef0a1cb39d84b81c5d06402a222f,
lives under desktop/controller-release-artifacts/playtest65-main-art-7869561e.
Its isolated update job has passed strict art installation and is compiling;
render/save proof and owner activation remain pending. Per-track artwork paths
preserve the new preview's own pack. Updates now prepare game payloads for the
existing console runtime; no replacement game Electron runtime is needed.

## Final repair checkpoint — September 19

- User review now follows the newest real revision: exact thumbnail and selected
  detail stay bound together; approve/reject moves the card immediately. Requests,
  Questions & replies, Approved / waiting to be implemented, and Rejected are
  distinct. V's exact-revision metadata puts old preparation in Library without
  losing originals, reviews or questions. Agent replies are append-only and never
  substitute for owner review. The question follow-up is active in V's existing
  task at five-minute intervals, quiet without new questions.
- Main update recovery: GitHub `7869561ee8b273796bebe82c803e9d477dd6a907`
  compiled with a separate, hash-verified 1,693-file legacy-consumer art closure.
  It passed native create/save/quit/Continue, exact identity and decoded appearance.
  Pack `main-7869561e-retained-art-06b10d898a07`; manifest
  `06b10d898a07ab335969cf3e34472b664bd8ef0a1cb39d84b81c5d06402a222f`.
  Immutable gen16 remains available for the combined preview. The updater now
  delivers verified client contents through the permanent console, with separate
  per-version art-package selection. Older source receives only exact verified
  private-input exclusions during provenance checks; unknown changes still fail.
- Native old-main to new-main save continuity passed. The subsequent offline
  version-chooser check exposed its missing-repository failure; the chooser now
  retains installed versions even without a configured checkout. Final native
  verification and installation are pending this checkpoint.
- Focused 68 unit checks passed, typecheck passed, and native tests passed except
  the sandbox-denied broker socket; all nine broker tests passed with loopback
  access. Browser Art Desk approval/rejection/questions passed. Calendar remote
  shifts use the existing campaign writer. W traced the opening optional meeting
  and its linked journey: the browser must explicitly decline that earlier bundle
  before attending a later shift. No confirmed commitment is skipped or return
  journey invented. Final browser and composition gates follow this checkpoint.

## O receiving — native controls and ready requests, September 19 evening

O owns final receiving. W regional eligibility `711cecb4` received as `2db48a11` by explicit O instruction, with additive receiving-record history preserved. U owns the native/bench changes in this checkout; O supplied the disjoint packaging-test improvement. No subagents, new workspaces or public publication.

The live canonical bench now contains four complete scene requests and V's three contact-review cards. Apartment instructions were corrected through append-only `request.revised` with an expected-version guard; original events, candidates and their original request versions remain. The ready queue requires a short exact prompt, model, reason/use and verified reference bytes. Full production notes and source metadata move behind optional details. The single reference has its own exact-byte download, and the normal request instructions name Add an image. Purpose filters use producer tags. A valid 6016×4000 JPEG exposed an undersized decoder workspace budget; the decoded-pixel and file-byte limits remain intact.

Native source now has a narrow trusted game preload, standard editing menu, Cmd+S and one guarded application Quit. Saving acknowledges both world and presentation persistence. All views give consent before any teardown; unknown old payloads require explicit consent, failed saves retain state, and interaction is suspended during the pending exit. Window Close hides the window; Dock activation restores it. Unsent question/edit notes guard Art Desk exit/restart. These native changes still require an isolated embedded proof and an installed successor before being claimed installed.

Executed: 79 focused store/bridge/decoder/cards checks, then 60 final store/cards checks including new readiness negative cases; 4 native-session negative/acknowledgment tests; packaging test; scoped lint and actual app typecheck plus test-import check (338 excluded tests are import-checked, not semantically certified). No full historical suite rerun or new pixel approval claimed. Owner app was observed on current main7869561 with an unsaved Astrid life; U did not close/discard it. A later independently initiated host restart refused the dirty local source, so restoring the same desk after freezing this source is the immediate next operation.

### Stable permanent Art Desk runtime activated

O reviewed and authorized the eighth registered workspace, owner ART-DESK / role runtime, at `~/Library/Application Support/Our Civic Duty Private/artdesk/codex-playtest65-u-e6eb5a3b68/source`. It is detached at accepted `db0e551b0ae8310b532d41b0cf57ae112ae519f0`, shares PG-LAND's Git object store, and has no tracked changes. Storage admission held through creation; measured footprint is 1,516,310,528 bytes. 17,059 private files reference the immutable generation-16 pack; 1,079 tracked files were preserved, dependencies reused, no private bank copied. Both pack/source manifests and entry sizes were checked; unchanged prior private-byte verification was reused, not falsely reported as newly repeated. Script and receipts: `/private/tmp/prepare-playtest65-artdesk-runtime.mjs`, `/private/tmp/playtest65-artdesk-runtime-prepared.json` and `/private/tmp/playtest65-artdesk-runtime-proof.json`.

Existing ArtDeskHost booted it first with isolated temporary records/exchange. With updater idle, only the permanent console state.repositoryPath changed; rollback copies are `/private/tmp/playtest65-state-before-runtime.json` and `/private/tmp/playtest65-settings-before-runtime.json`. Native Restart Art Desk then loaded the same canonical store, including new owner events through seq626, and preserved standing candidate `cand-20927148-a73e-45f5-872e-32c6a996805d` and selected Needs review tab. Actual origin `http://127.0.0.1:58945`, helper60039/Vite60042, source db0 and digest c453a32558eb843b90dd011f67fd728f6cb274bbe5d1ef4087ebf14f6975aa4c. Identity dirty=true denotes private untracked input manifests/symlinks; tracked-source status is clean. Native host remains c14cb9d3, selected game main7869561, private preview cachedf4029d65; new native commands are not yet installed.

O's accepted W chain received after isolation: 8c8e6064→291dd893, 67ca6580→a30b30e8, 40ac591d→d05f1405. Only U's writer checkout advanced. No public/main merge. V was told new API identity and asked to answer three new owner notes directly; no owner review decisions were authored by U. Apartment candidate cand-8130c973 is an unattributed upload matching the reference's exact hash, preserved without asserting it is a new repaint.

### Composed native proof preparation

The stable desk identity remained unchanged after the writer advanced and became dirty (`/private/tmp/playtest65-artdesk-runtime-independence.json`). Producer review views now read Clean image / Annotated image / Contact close-up, with generic diagnostic entries called Reference image. The game menu accurately distinguishes returning to the title from quitting the native app. Shared source manifest was regenerated once after the accepted Portland chain. All pinned private-pack consumer files still match the prior f402 compatibility receipt; originals remain unchanged.

Focused card/regional checks passed 25 tests; native acknowledgment/unknown-session/mixed-cancel tests plus packaging passed five tests. Scoped lint and formatting passed. The composed typecheck caught an optional inherited-tag access in the small label helper, which was corrected; the build below will rerun the typecheck. `desktop/scripts/smoke-test.mjs --native-session` adds isolated native Close/restore, Cancel, explicit failed acknowledgment, actual native Save, deliberately delayed real persistence acknowledgment and Quit/reopen checks. Dialog selection and failures are test injections in the disposable process; menu callbacks, preload, game writer and IndexedDB persistence are real. Execution and final delivery are still pending this preparation receipt. O owns final receiving/activation.

### Native combined build verified — dd07036c

Exact product source `dd07036c9268853eafd09b319a491b7dbfa4427c` compiled successfully with application typecheck and test-import check. Staged private client tree `0d2875da8cd4dbf2ad4d3879d6825017d2c93750b6f7907113860dc19966365d` includes exact approved White House revision 6. Packaged native host archive SHA-256 `22dbf4da4362f5f70c1f452942d82de5a96e015255f0c630b359f6fa84ee8cb7`. Both are preserved/protected under `desktop/controller-release-artifacts/playtest65-native-dd07036c`; the payload is `combined-client.app`. This is ready for O receiving, not yet installed in the owner's console.

The first native test was refused before launch because disposable output reached 6.6 GiB. O authorized a same-filesystem MOVE of only the 1,933,744,071-byte pending deliverable from its test run into the existing receiving directory. No active reference existed, destination was absent, and client hashes matched before/after. The ordinary guard then passed; no override or deletion occurred. Historical 3.7 GiB rollback/proof remains. Location receipt: `test-results/runs/playtest65-native-dd07036c/delivery-location.json`.

Two first native runs exposed a harness flaw: a late save-event listener could register its hold after the production writer had already captured completion. The bounded test correction wraps completion before real dispatch, restores dispatch immediately, and holds the actual writer's result. Electron retains before-unload ownership; unexpected browser dialogs fail. Product controls were unchanged. The admitted rerun passed Close/restore, Cancel, explicit negative acknowledgment, actual menu Save, held actual saved=true with app alive/inert, forwarded acknowledgment and real host exit, relaunch/Continue, same save/world/person/appearance, complete interface and decoded art/material hashes. This is a negative-ack test, not a disk-full claim. Receipts (including failures): `test-results/runs/playtest65-u-checks-dd07036c`; successful final receipt `native-save-wrapped-ack.json`. Isolated save root: `/var/folders/0s/plfg27pd01550kb21zrmkwn00000gn/T/ocd-smoke-aQZyut`. Opening capture was inspected; this grants no new artwork approval. Harness correction is recorded separately from unchanged dd07036c product bytes.

W regional selector remains source-ready, not mounted in player UI. V has now received Pikeville images but no accepted exact candidate leaf has arrived in U; request references alone are not regional gameplay art. Canonical desk remains pinned db0 and owner decisions/notes are preserved. V verified the two reported missing reference originals; UI currently collapses their section after upload, motivating a separate compact-reference follow-up.

### Installed console and compact reference follow-up

The permanent `/Applications/Our Civic Duty Private.app` was replaced at the same path with verified dd07036c host; the private preview client is also dd07036c. Existing install ID, one Dock entry, main7869561 selection, settings and canonical history were preserved. Actual native version selection loaded dd07036c with enabled Quit; main selection and the same Art Desk review were restored afterward. Installation/native receipts and rollback are protected in `desktop/controller-release-artifacts/playtest65-native-dd07036c`. Native save/reopen proof is isolated; no owner life was created or discarded.

O's accepted D.C.35965 and Portland date50ea are composed as92f6ca92/67522798; shared manifest regenerated once. W's separate coverage-description correction is still pending; current municipal manifest accurately counts five records but retains stale three-government coverage wording and its existing evaluation horizon. No municipal interval work is included here.

Compact original references now remain visible after a return, outside collapsed editing instructions. Exact download, pointer and keyboard checks passed at1440×900,1280×720,1024×768 in disposable Art Desk records; actual1024 capture was inspected by U and O. Test root `test-results/runs/playtest65-u-visible-reference`. Clean/annotated/contact view labels from dd source are included in the upcoming runtime promotion. This paragraph records source verification, not yet live Art Desk activation.

O's single Pikeville candidate leaf is pending/unreleased, exact hash2cfa09f644d30749d8a129d7441f9bbde46b0b5875c1e9e8c6a253cd85e5377f, encoded2496×1664, native-detail unverified. No raster tier was fabricated. Candidate-only information consumer requires real available bytes and matching descriptor/manifest hash, scopes this leaf to localPikeville2160852 inMay–September, and excludes the state beat, January and other towns. Positive June browser fixture mounts the real opening panel, preserving3:2 at420×280, with no cropping/overlap at1024/1440. It is fixture reachability, not a claim that the January normal start shows greenleaf art. No world/time/presence/approval write. Failed first browser harness dependencyURL attempt is preserved; ordinary Vite-resolved support fixture passed. Captures: `test-results/runs/playtest65-u-regional-browser-mounted`.

Focused30 checks plus9 composed municipal/regional checks passed; municipal exports byte-identical; source validation24domains passed with existing warnings; application typecheck and340excluded-test import checks passed (not semantic coverage of those tests). Scoped lint passed. `validate:art` passed, full `inventory:art` examined11,999files with historical duplicate warnings, `qa:art -- contact art/generated/candidates/art-desk/playtest65/regional` passed. Outputs are under `test-results/runs/playtest65-u-regional-reference`; tracked historical QA files were restored byte-exact. New pixels remain private and pending. O owns receiving and final composition.

O then read canonical owner events755/756: the other town return09f46631/950128 needs road-mark revision, and intended eligibility is regional type rather than a Pikeville-only contract. The earlier434644 descriptor and bytes remain banked as technical evidence but the default regional candidate list is now empty. The existing preview fixture explicitly supplies the inactive banked leaf; no replacement, new approval or wider geography is inferred. V handles the requested exact-source child revision; W identifies the existing context seam. This follow-up keeps the ready reference controls independent of that artwork decision.

W's accepted coverage correction288eb27b received as9b539321; shared manifest regenerated to match all five bounded records, dates/facts unchanged. Superseded candidate deactivation passed eight focused checks, then the explicit June fixture passed again at1024/1440 with scoped lint and application typecheck. Ordinary default candidate list remains empty. Ready Art Desk promotion uses the same existing runtime and a frozen successor; native dd host/main remain unchanged.

### Art Desk a5db installed; Notifications verified

The same registered runtime was promoted fromdb0 to a5db37ccea3bd7099c088680711419efcaa92b87 and reopened through the same native dd console. Actual reference thumbnail/download and collapsed editing instructions were visible. Canonical storeb40dfbc0-bcee-4749-b570-1c9db4591e1a retained the exact806292-byte event prefix SHA6bd9b1ae1804d33f36ae0b8170f38980eb4ccc6aaa7d43c1517e3c28258c3a6f. Main786 selection and hostdd stayed unchanged; receipt `/private/tmp/playtest65-artdesk-a5db37cc/promotion.json`. Native generic old-client warning initially caused automatic approval rejection; after Keep Open and fresh proof of both title-only games/no saves/empty drafts, the guarded retry succeeded. No process kill, discarded owner draft or game bypass occurred.

O relayed the owner's explicit Notifications request and supplied the disjoint pure reply projection/helper tests. U added an Art Desk Notifications section with unread count, text/time/artwork context and exact-revision conversation links. Per-event read IDs live in the same store's durable `preferences/notifications.json`, outside disposable cache and canonical art history. Existing loopback/origin/host-token or session-capability controls apply; unknown/future/nonreply IDs are refused. Atomic per-ID union retains other tabs' reads. Existing15-second refresh delivers incoming replies; no second poller, OS notification or game effect. Read-save failure retains unread state with retry text. Current question/edit/revision drafts stay mounted while viewing the feed; jumping away with a draft is blocked with Back to artwork. Read-only request cards now persist selection through the existing native preference interface.

Executed47 focused helper/store/bridge checks, scoped lint, application typecheck and341excluded-test import checks. The first helper test exposed an inbox title falling back to the newest filename; corrected exact-candidate naming passed unchanged assertions. Browser proof passed at1440×900,1280×720,1024×768: injected503 leaves unread, keyboard opens exact earlier revision and focuses reply, read status survives reload, draft survives Notifications/back and blocks destructive jump, external team reply arrives through the ordinary refresh without Sync/reload, Mark all read records only displayed reply IDs. Last proof `test-results/runs/playtest65-u-notification-arrival` passed35.4seconds; actual normal-status1024 capture inspected. Prior failed and passing receipts retained. Notifications source is ready for O receiving and same-runtime promotion; this paragraph does not yet claim it installed.

### Notifications installed; regional composition prepared

The permanent console's same Art Desk runtime is now pinned to2a9a258cde9551603e835b12bd3a88c095c3f4af. Actual native Notifications showed18unread team replies; none were marked read by U. Same selected artwork, canonical store and851085-byte history prefix preserved. Native host/private cached game remain dd07036c and main7869561; source changes below are not yet installed game bytes. Promotion receipt `/private/tmp/playtest65-artdesk-2a9a258c/promotion.json`, native feed inspected inline and O accepted.

O accepted W regional/interval chain1650466a/a7e27d43/5d846e8f/47486fcf as U f7433a49/d5498426/23d57cfa/c99949e0. W narrow jurisdiction repair a0677c6f received as6c1601d0: government identity no longer follows its holder's residence or requires a holder. Next W municipal-holder work remains separate from this composition.

O supplied/QA-accepted the two new private leaf rows: town street correction85720edf325911f8bbc741c2c88fa8b23cb40ba237e455390f6e4e1341515af0,1536×1024; prairie0df680c304a6ae6d0d7a1e7d8dfe43f35a8236ef19f696e191ba3207e98d5814,2496×1664. Both remain pending/unreleased with native detail unverified. Old434 source/history remains banked. Regional eligibility is independently reviewed type context, never inferred from artwork tags or blanket states. O art validation/inventory/contact checks passed in `/private/tmp/playtest65-o-region-types-qa/results.json`; historical tracked reports restored unchanged.

U corrected the old fixture to use canonical advanceWorld with the existing campaign/life transition registry, serialize/reload the World, and assert unchanged saved bytes after panel navigation. The first run exposed the omitted transition registry and stopped; retained evidence is `test-results/runs/playtest65-u-regional-types-review`. Corrected8-case browser run passed2.1minutes in `playtest65-u-regional-types-handlers`: Pikeville, Hazard, Fort Pierre, Cottonwood Falls, Cody; unknown place, winter and ordinary production negatives; state-beat refusal; keyboard/pointer,1024/1440, exact source dimensions and save read purity. U inspected town and prairie1024 captures. This is explicit later-season fixture reachability, not a claim the January normal start shows these summer images.37composed tests/6suites passed, including new missing-byte/hash/production cases and W identity regressions. Scoped lint/format/diff passed.

After that proof, the default private candidate list now selects the two received regional-type leaves and visibly captions each Regional illustration. Final default-path browser and private build are next; this checkpoint grants no owner art approval or production release. Immutable gen16 inputs and dependencies are reused; no workspace or new native package is created. O retains final receiving; U prepares a concrete guarded client-only installer for review.

Overnight coordination: O reports heartbeat `our-civic-duty-overnight-delivery` active every15minutes for40runs in O's existing task; V's existing30minute reply follow-up remains. O's finite caffeinate -i -t36000 PID84270/session7974 was verified by O; no permanent power-setting change, duplicate automation or additional keep-awake process was created by U.
