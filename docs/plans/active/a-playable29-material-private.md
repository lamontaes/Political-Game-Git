# A prepared materials — local internal review only

Consume B ed20def7 and exact V d285bd57 after current A B/G/C composition.
Preserve the existing two-site marker adapter, all older generations/replays,
loaded saves, root mounts and scene/focus repairs. No additional root patch.
Receive Desktop's archive and material/CSP proof commits 2f6e8988, 53919a29 and
36b86320; actual installed/controller source remains separate.

PUBLICATION BLOCK: automatic review rejected B's public push because source
and derived asset distribution rights are unknown. This branch and its assets
remain local. Do not push, dispatch public containing-artifact CI, upload or
use another branch/archive as a workaround. Explicit authority is required
before publication. Existing controller Update only targets accepted main;
use isolated direct local app/internal-review proof without user-state hacks.

Mount the unchanged material caller proof in e2e. Verify actual native decoded
SVG IDs, persisted material JSON/content hashes, all six body families and
preview cancel/confirm, real swaps, saved/reopened identity, old-generation
isolation, protected pixels/alpha and bounded cache. Passing technical proof
does not accept the faceted art finish or infer rights. Keep last-good9f,
actual6e state/profile and earlier frozen players intact. Send exact local
source to existing Desktop/H as a named internal-review checkpoint.
