# A39 T current-source service receiver

Status: active finite endpoint, not ordinary paid acceptance.

Preserve T a518 and the initial exact F c315 production intake separately.
Use A's frozen current 7d53d9f1 receiver base; receive only F/T net, optional
fiscal fields and feature-local consumers. Preserve newer S/L/X, N lifecycle,
ordinary LIFE clock, pack definitions, PlayerGame and K fields. No public upload.

F's exact measure/source/input transfer to S is now approved and acknowledged
in S116f60b. Do not repeat the approval request. The separate revenue decision
source is still absent; identity and eligibility are not legal execution.

Deliver the actual public-account/appropriation path if valid, one paid stage,
cancel/duplicate/refusal/reopen, or the exact failing operation and smallest
producer dependency. Reuse unaffected funded-service evidence. Preserve the
first/second-period exact cent remainder and existing cash snapshot. Thin leaf
integration and a focused browser proof only; no broad tax/travel expansion,
root/CSS rewrites, treasury or political-effect claims.

Return committed feature-local net against 7d, exact root mount contract for A,
tests actually run, source identity and explicit acceptance boundaries. Stop
after the finite return; primary/installed/human acceptance remains with A.
