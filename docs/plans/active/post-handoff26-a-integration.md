# POST-HANDOFF26 A integration

## Authority and preserved state

- Continue the existing A owner under CURRENT CONTINUATION, SHARED CONTRACT,
  and SECTION A of NEXT24 (live document read 2026-09-12).
- Sole shared-root/main integrator; use this isolated successor worktree.
- Preserve the root's two user-authored PNG changes, port 5188, saves, all
  originals, active owner worktrees, and frozen PR #213 source.
- Frozen checkpoint: `f2dded2895aba1f55b741d55986a576f1e556b81` on
  `codex/morning23-a-landing`; its required exact-head CI is still running.
  Do not recreate #144 or relabel historical `8caa4216` proof.

## Small useful increment

1. Bank #213 after its current required gate passes and verify main containment.
2. Integrate only ready net #211 title/creator changes and B's later useful
   continuity proof, retaining A's NPC-read-only / Personal-self correction.
3. Inspect #217's approved grounded-content/school/presence subset and classify
   current browser failures before integration; test opening-introduction
   presence separately from later room presence.
4. Keep #216 source authority with the existing desktop owner. Confirm its
   News interface-v3 transfer compatibility, v1/2 migrations, future-version
   refusal, and exact artifact identity before inclusion.
5. Recover current D/E receipts without commissioning duplicate semantics.
   G candidate engineering remains distinct from art approval/promotion.

## Verification and delivery

- Reuse unaffected accepted evidence; exercise changed title/new-life,
  scene/person/conversation/browse/return, controlled-self wardrobe, and
  Keep/reopen routes. Add desktop transfer/reopen only if desktop is included.
- Run focused checks during iteration and the repository-required gates on
  the frozen publication head. Preserve pointer/keyboard assertions.
- Report code delivery, main containment, installed artifact, and human visual
  acceptance independently; do not claim final art acceptance.
- Keep one identified usable frozen play source while later leaves advance.
- Publish a safe cohesive increment without waiting for all poses, logos,
  geography, or later systems. Finish with exact SHAs, clean state, actual
  tests, protected blockers, owner-scoped follow-ups, and a small LEARN pass.

## Integration receipts and contained audit

- #211 exact `6272545b` preserved by merge `fcc3ae47`; regenerated the
  combined corpus instead of choosing a donor's generated counts. Its changed
  title/creator/hover/opening browser selection passed 23/23. Lint, formatting,
  typecheck and release validation passed on that checkpoint. Broad corpus
  anchors: 167 passed, one live scan exceeded 5 seconds; the unchanged tree's
  complete corpus file passed 42/42 on isolated rerun. No assertion weakened.
- Idle B `4b134310` net recorded as `f386e7f3`: distinct old unpinned/gen2 Worlds,
  exact per-person recipes and rendered layers through migration/reload.
  Retained A's Personal/self ownership, talk-back and navigation-owned dismissal.
  Active PEOPLE/B visual work remains separate, with its candidate-creation
  seam reserved for that owner's eventual frozen return.
- #217 exact `18813173` preserved by merge `8664cc31`; approved source subset
  retains #198/#202/#165 grounded premises and existing Calendar/Places reuse.
  Combined corpus: 2607 templates, 6250 unclassified candidates, zero hard
  errors, 265 warnings. Donor's current failing selectors/model fixture are
  already corrected by A; changed school and persistence paths still get
  combined-tree proof.
- Opening presence was independently tested, not inferred from continuing
  story tests: household introduction received school context. The root now
  reads the actual introduction/opening scene until the foreground enters the
  continuing life. This is a contained presentation correction, not a new
  presence engine. Audit disposition: context/supersession corrected now;
  canonical history, clock, IDs, replay, simulation purity and asset release
  boundaries confirmed unchanged. Unknown school/neighborhood plates remain
  absent rather than being substituted with home art.
- Desktop owner confirmed interface-v3 gap and published codec repair
  `0fefe29e`, reporting shell/World 71/71 and desktop contract 33/33. Package
  full-interface reopen/update proof and final artifact receipt remain pending;
  its worktree/source authority stays with that owner.

## LEARN

Validate the room against the foreground's own scene contract. Passing the
later story's presence tests does not prove that an introduction mounted over
the same root shows its household. Keep read-only context resolution and a
separate opening browser assertion at that shared mount.
