# Desktop PLAYABLE29 delivery

## Prepared material proof

The material follow-on must be composed with B's catalog-generation-5 return,
not applied to the earlier safety-only package. The existing Mac review smoke
requires a genuinely saved material, decoded layers, every marked layer's complete
parameters equal to `appearance.material`, and unchanged asset IDs/native SVG
SHA256 across clean quit/Continue. Blob URLs are deliberately not identity.
Discriminating controls reject wrong person/seed, missing/undecoded layers,
loading state, changed palette/features/version and missing SVG identity.
This is packaged decoding/continuity evidence, not human visual acceptance.
Use A's next accepted useful frozen composition, not the old #228 package.
Initial preparation is clean source a50b005016269cd8b28833c4fe119f676f8e9c0e;
A remains sole root/main integrator. Existing controller, durable close,
interface-v3 codec, installed owner state/profile and last-good app stay intact.

Deliver only the mode/symlink-preserving private ZIP and checksum as a dedicated
Actions artifact; never offer an expanded artifact-service `.app` as installable.
No release/feed, signing purchase, semantic version bump or launcher rewrite.

Package the exact accepted source/profile/arm64. Verify ordinary candidate play,
complete two-life World/appearance/interface continuity and visible Play/Update.
For B's material successor compare drawn asset IDs, complete saved parameters,
decoded native SVG hashes and ready states across reopen, not changing Blob URLs.
Existing desktop CSP already permits `img-src blob:`. Six-family acceptance and
human visual approval remain separate from decoding and source/runtime proof.

Prior useful checkpoint is compiled 9f16ab58d0c05895dbcf0d94028653c768797f74,
not a50/P29 materials or accepted-main merge efb227f2. All three package matrices,
current smoke/complete-v3 transfer/reopen/refusal passed. Native 28-to-9f visible
Update, running-game retention, normal quit and keyboard Finish launched exact
9f on a disposable profile; both complete before/after UI exports matched.
First normal life reopen was exercised. Native automation's repeated stale app
tracking prevented claiming the second normal reopen; exact-9f CI portable
two-slot reopen passed separately. Never publish raw fixture/player saves.

LEARN: usable Mac delivery is the archived bundle with its modes and relative
framework links, not a directory reconstructed by the artifact service. Build
identity remains the compiled source even when later test/docs are equivalent.
