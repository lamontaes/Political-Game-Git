# ENV-FINISH4

Continue the recovered ENV lane on `codex/env-all1-transfer`, PR #142, from
`4ff9282e92d885db3b8834e8a0d2468687370df7`. Fresh main at preflight:
`6b95f3713590f18973e5e54a3da86e6cd66aa734`.

Authority: FINISH-WAVE4 shared execution contract and section F, plus the full
original ENV-ALL1 assignment. No scene-count or first-play cap. Original Claude
checkout and recovered masters remain read-only. Preflight passed; process
inspection found only the preserved source Vite server on 5216, no heavy gate.

1. Preserve #144's existing activity/scene connections; finish MUNI's actual
   attendance callback with its source owner and sole UI-root owner. Verify
   participant, location, clock and reload behavior on the combined checkpoint.
2. Extend supported scene/context and place/provider consumers from existing
   corpus evidence. County selection cannot synthesize a town; multi-county
   places must retain all supported relationships.
3. Complete usable existing geometry/occlusion/slots and candidate review
   visibility, retaining source hashes, native-detail lineage and real approvals.
   PEOPLE owns person composition, UI owns root/layout/shared integration.
4. Run focused regression controls, mandatory art commands and a coordinated
   normal browser gate. Publish exact frozen adapters and precise remaining
   source/pixel/approval gaps on this same PR, without self-merge.

Read-only helper `geography_seam` inspects only existing geography/provider
evidence. Parent retains implementation and integration responsibility.
