# Local-only material focus follow-up

Desktop requested the exact local BF9 material composition consuming A's narrow
9aea menu-Escape repair, not a duplicate public C-only package or art export.

Preserve the entire frozen BF9/V/B tree and World/save/interface behavior. Apply
only the identical menu dismissal callback/key handler and add actual candidate
menu-Escape/reopen/Talk proof beside existing native-material caller checks.
Regenerate derived prose without changing anchor history. Run affected checks
serially, distinguish actual source from compiled BF9 archives, and hand off a
clean local-only SHA. No upstream/push/Actions/user-profile changes.

Public C9aea publication approval remains separate. All V/B material provenance,
rights-unknown/internal-review-unapproved status and exact permission scope
remain unchanged. Existing BF9 archives retain their known menu defect until a
distinct future local source is actually compiled/proved by Desktop. Native
harness authority and human acceptance remain pending.

Local9e080 release guard failed on the inherited Desktop declaration `summary`
key. Preserve that red and receive only the same declaration-body/explicit-URL
adapter already checked at public20ef, with native package assertions unchanged.
