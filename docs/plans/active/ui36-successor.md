# UI36 recovered successor

Base: d8fcd00bf77093a70af921ae8bef6d5a8db25efe, with recovered untracked ui36-shell.test.ts.
Workspace: /private/tmp/fable-recovery-20260914/UI36-successor, branch codex/ui36-successor. Original and complete preserved clone remain read-only; ignored/private inputs copied unchanged. No upstream; fetched origin/main f22fd314e72bec0440044026ccdfd99e7a67600d and origin/pr-245 559b1c043a45205bdaa66ea25daa6cc9643da99c.

Scope: complete UI36 shell, navigation, person/contact and interruption adapters and focused proof. A integrates; STYLE owns appearance controls and art runtime; LIFE semantics stay with LIFE.

1. Inspect owner sketch and reproduce baseline screen on isolated port 5291.
2. Diagnose recovered failing adapter assertions from actual records. Repair only owned UI behavior.
3. Run two focused gameplay journeys, pointer/keyboard and short viewport checks, changed-file hygiene and build/type checks.
4. Freeze a precise successor delta and evidence for A through parent.

## Frozen implementation

Commit `3a41b37e05c4d5b7d6fea997cf486baba807b53e` fixes only the calendar outcome target and preserves the recovered adapter proof. The canonical day writer still advances to the requested date at 07:00. The presentation now measures that target rather than assuming 1,440 minutes from any start time.

The recovered tentative test originally selected the first scheduled hold (January 6 at 18:30) while simulating only through January 6 at 07:00. It now selects the actual same-day hold, asserts the exact stopped moment and cancellation state, and rejects the false successful-skip interruption message. The original child walk fixture had one recorded guardian, who accompanied the player; the supported-return fixture now has an independently recorded family member remaining home. No domain writer was changed.

Executed: recovered focused Vitest file, 4/4 passed; changed implementation/test ESLint passed; changed-file Prettier passed; `git diff --check` passed. Diagnostic fixtures were removed after inspection. The previous Fable browser log is historical only: it contains 18 failed/22 passed tests and source drift from a33bdce8 to d8fcd00b.

A is receiving the frozen delta while owning combined build/type and final two-journey acceptance. The successor's single ordinary Aurora, Colorado age-34 birthday-April-12 Chrome journey is staged separately; it uses the candidate-art route and no private-save injection or seed-shopping. No duplicate heavy build or art run is authorized during the shared test slot.

LEARN: calendar outcome assertions must select the event inside the actual advance horizon, and compare the writer's canonical target rather than an assumed day-length duration. The recovered focused regression encodes both lessons.
