# UI-SAVE5

Sole UI owner, PR144, codex/ui-core-release-transfer. Starting source d24c44e77782dd2fb2781822a1e51b55dae40666; accepted main04c735c32c8e38d942d5ba978a2efc5dd63315bb. Read CLOSEOUT-RETURN5 section1, ACCEPT-CLOSE4(c) and correcting addendum. Preserve original audit and prior proofs.

Current-head before control: identity-verified ui-save5-before, port5371, production-play plus persistence-cross-tab:7passed/11failed in2.7m. Ten failures are Keep assertion; the cross-tab raw inspection instead fails with VersionError because it explicitly opensv1 after the app createsv2. The inspection will open the existing version without requesting a downgrade. Original reviewer control34003c50 is preserved; this new run is separately attributed.

Initial Keep creates a slot; repeat manual Save updates that slot. Distinct actions/test IDs must preserve this difference, awaited persistence and same World/person/save identity. Keep's disappearance assertion stays; repeat Save is explicitly tested. Cross-tab deletion/conflict never silently overwrites or resurrects another tab's slot.

Reconcile OPENING against accepted split identity/wording controls using an explicit reviewed leaf delta and30 source-created personality/value/goal records, originalseed unchanged. No opaque digest recapture. Finish exact press and other frozen normal adapters, run affected full/browser gates, capture current normal scene/dossier and clearly review-only wardrobe artifacts. Human/independent acceptance stays separate; no merge/activation/monitor.

## Current integration correction

The frozen 3202bad46c87375f8a9431cee62b4007cfc8d95d browser matrix completed
66/75. Initial Keep/repeated Save, store cross-tab checks and owner-review
screenshots passed. Seven failures exposed stale database-version inspection,
reload-phase/text-position assumptions and the LIFE combobox locator. Those
checks now use the existing database version, propagate storage errors, enter
the same day surface and compare complete replay Worlds. A new two-page normal
player test exercises stale-tab refusal against the actual saved World.

Two unresolved failures are substantive integration boundaries: the normal
custom office start creates legislative staff, while the corrected legislative
adapter requires a supported member seat to introduce a bill. Preserve that
refusal; no fabricated seat or relaxed domain rule is an acceptable test fix.
The previous News and docket filing proofs remain recorded as failed on this
head pending the existing owner's supported normal-entry interface.

Consumed ECON 926b036c3aa99659b2cb4366e60a1a9764d525e2's exact endpoint-release
interface, rendering and tests (not its stale generated prose reports).
Consumed MUNI c9f0a44b4ff30703d095ea030e9729708a697b45's source, panel, adapter,
projection, negative tests and scanner correction from its immediate parent.
Combined anchors were allocated with `corpus:prose -- anchors`, then the corpus
was regenerated. Generated CSV trailing spaces represent literal source
fragments and are retained. Focused donor plus OPENING controls: 68 passed.

LEARN: browser storage inspection must open the existing database without
pinning an obsolete version and must propagate errors rather than report empty
records. Presentation timing is not World identity; compare serialized Worlds.
