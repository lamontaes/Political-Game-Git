# OPENING-REPAIR6 blocking source delta

Patch base: OPENING `e890e4d1822481a00a8111a021e66b0c177ca3d1`.
Exact combined proof base: UI `1119e890c471fbf83b4cd592aabe6b28c5f075b2`.
Apply `source.patch` as a patch, not by replacing files or merging older donor ancestry.

Four source paths:

- src/presentation/new-game.ts
- src/presentation/production-world.ts
- src/presentation/life-scene-flow.ts
- src/simulation/opening-life-content.ts

Reproduction: UI's unchanged adaptive-life and dialogue-reachability files failed exactly the three reported assertions (39 passed). Donor repair: those files plus scene/agency tests passed 58/58. New exhaustive branch test passed 20/20: every existing added family, every original answer, positive and negative follow-up eligibility, saved counterpart, reload, distinct continuation outcomes and no read mutations.

Combined: apply only the attached four-file patch to UI1119, add opening-repair6.test.ts, run adaptive-life, dialogue-reachability, ordinary-conversation-integration and opening-repair6, one worker: 68/68 pass. UI's original identity hashes, 69 explicit initialized-preference leaf deltas, corrupt ID/reference/wording controls remain untouched. UI roots and other donor additions remain untouched. No seed substitutions, catalog exclusions, count equalization or opaque snapshot recapture.

Expanded donor run: all four behavior files passed, including 24 opposite-calibration comparisons at ages5/10/16 using actual membership state kinds/roles/status. Corpus tests identified one remaining transcript coverage failure (`election-lost` not demonstrated); its assertion remains unchanged. A scanner pin drift caused by editing the test source was remeasured from actual regeneration (53,771 literals / 2,115 inventoried / 367 files), never guessed. Generated corpus: 2,281 templates, 3,493 unclassified candidates, zero hard errors, 268 warnings. Complete classification is not claimed.

Existing breadth assertions still require every original moment exactly once. Their walkers now traverse answer-dependent stages too, and daily recurrence counts moment openings separately from their continuations. Old saved openings without an opening-stage tag remain moment stages. No generation or identity baseline was rewritten.

Acceptance: focused source/combined regression proof, not human visual acceptance or full original-scope completion. LAND retains the heavy/browser slot. UI owns integration and final hosted gates. Original unmapped source kernels and extended officeholder coverage remain unfinished, not external blockers.

LEARN: family topology must consume the pre-calibration identity stream while the approved bounded age draws retain the build stream. Keep both an actual membership-state comparison and the existing calibration controls so undefined property projections cannot hide topology drift.

## Continued normal-player proof

After the blocking publication8fdbe588, an added normal-consumer lunchbox test showed an eligible direct continuation could be postponed by seeded scene selection. A bounded follow-up now prioritizes only an eligible continuation of the previous family; unrelated selection still uses the identical seed/hash path. Saved lunchbox and scene-breadth tests pass30/30. The additional consumer test retains counterpart, distinct stage/event identity, actual choices, save/reload and zero-write reads. Separate combined rerun is recorded in combined-final.log. Final scanner measurement after this addition:53,786 literals /2,115 inventoried /367files. No original identity hash or seed changed.

Art validation, inventory and QA commands ran successfully. These do not constitute human visual acceptance. Logs are explicitly tracked despite the repository's general log ignore rule.
