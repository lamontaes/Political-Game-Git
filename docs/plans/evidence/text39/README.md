# TEXT39 delivery receipt — partial

Source: A's recovered `7d53d9f1c6898ec77eba479c1692b9c896fbbbac` in isolated `/private/tmp/pg-text39`, branch `codex/text39-player-prose`. Household increment `b94e12af5c5aab929954dd70b919c53599942671` was received by A39 as `7f3477cf`. The second increment is separate from A's stopped delivery. Remote `origin/main` was re-fetched at freeze and remains `f22fd314e72bec0440044026ccdfd99e7a67600d`. Before/after uses the same deterministic ordinary-life replay, not a recovered copy of the original `hhhh` save. No private art was published; no TSX/CSS or #247/#244 implementation was replaced.

| Surface                                | Changed source and actual consumer                                                                                                                                                                                                                                              | Evidence                                                                                                                                   |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| Household                              | `life-opportunities.ts` saves groceries, fitting call and cupboard-repair call, with no appointment times. Adult and conversation readers use that saved summary; old saves retain theirs.                                                                                      | [Full before/after choices and replies](household.json). Actual location: **Home**, not an invented kitchen.                               |
| Child opening                          | `opening-life-content.ts` choices/followups and `life-conversation.ts` replies feed `life-scene-flow.ts` and the ordinary talk writer.                                                                                                                                          | [Tag scene, every answer, both continuation choices and dialogue](child.json). Actual location: **School**.                                |
| Civic dialogue / active adult requests | School/neighborhood response banks no longer invent previous meetings, offscreen work, props or elapsed time. The notice reader uses the saved work summary. New extra-hour and agenda requests receive prospective facts in existing writers; legacy fallback remains generic. | [Ordinary neighborhood dialogue, every answer and next reply](dialogue.json). Actual location: **Lexington, Kentucky**.                    |
| Questionnaire                          | All 56 selectable items have revised prompts/options and `.text39-v1` keys; bank is `pg-setup-bank-v5-text39`. `questionnaireScreenFor` reads that bank. Eligibility, old answer records and option metadata stay intact. Partial old runs cannot reask a revised counterpart.  | [Concrete examples, full options and next-question output](questionnaire.json). All six age/depth paths and their shown answers were read. |

The old debate/graduation “only one / attend both” item and overnight grant escape were already withdrawn on the recovered source. They remain readable for old answers and are not selectable. The replacement family/interview scene explicitly permits **partial** celebration attendance while missing the speeches; alternative-site and assistance choices ask rather than guarantee a solution.

Verification actually run:

- Focused opening/PLAYTEST34/action-memory run: 54 passed, one obsolete privacy wording assertion failed. After updating that literal, its bounded rerun passed (1 passed, 16 deliberately unselected). No behavioral assertions removed.
- New old-save regression: 3 passed after correcting the picnic fallback. Conversation-commit and P2R1 grounding suites: 27 passed; this closes the two earlier host-load timeouts without changing time limits.
- Direct ordinary household/child/neighborhood proof scripts: all branch, unchanged-clock where applicable, immutable-render and save/reload assertions passed. Questionnaire proof: 56 items; short/deep at ages 6, 16 and 34; every shown answer and next question.
- Browser at isolated port5293: actual pointer answer, Enter answer, and Enter early exit reached gameplay. Read all displayed options for furnace, trip fund and reference questions. Screenshot inspected. This is agent verification, not owner visual acceptance.
- Typecheck, scoped ESLint and diff whitespace checks passed. Prose probes 23/23 behaved as specified; holdout hygiene passed. Independent grounding reviewer passed the bounded changed claims, excluding the surfaces below.
- `validate:art`, `inventory:art`, `qa:art` passed; inventory reported pre-existing duplicate hashes. No art changes.

**Still open:** all 19 formative scene rewrites (the rejected draft was removed), broader hosted92C/episode banks, complete office/legislative dialogue, and full human prose/visual acceptance. In particular, formative object/cause, argument, rule, activity membership, disputed claim and organizing problem need explicit source facts. Existing school-project conversation premises still need their own source-fact repair; the revised response bank does not certify those premises.

UX received `text39-ui-copy.ts`. A verified Calendar, Month, Week, History and date-format wording in its actual components; it did not import the module or adopt every other string. The questionnaire introduction still displays the old “These are about you, not your character” paragraph. Creator/save/travel/empty-state and questionnaire-introduction adoption remain with UX; this work did not race those component edits.

LEARN: new specific copy must be tested against older event versions as well as fresh writers. The small `text39-legacy-text.test.ts` regression makes that recurring failure inspectable without another larger prompt.
