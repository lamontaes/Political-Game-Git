# DELIVERY28 Budget → A

Receiver: A, task `01a095e9-50d7-7923-9fb8-012c8964431f`
(`Finish MORNING23 A landing UI`). Continue existing [PR #222](https://github.com/lamontaes/Political-Game-Git/pull/222)
on `codex/recovery25-budget`; preserve `72c48b59ee9554ed4c800d3c9b7df51e9f263620`.

## Current isolated integration

Current-main receiver: `/private/tmp/pg-budget-delivery28`, local branch
`codex/budget-delivery28`. It preserves the #222 head and reconciles accepted
main `2d154752aa38c9b94227b53ecfffb2db58081f9a`, containing landed #227.
Only the adjacent Budget/SaveTransferControls import conflicted in PlayerGame;
both imports and all current identity, Personal, wardrobe, News and transfer
consumers are preserved. Combined prose artifacts were regenerated from the
receiver rather than choosing the old packet. Main and A's checkout were not
modified by this owner.

[The current three-file Politics adapter](../handoffs/delivery28-budget-current-politics-mount.patch)
is the exact root delta against that accepted main. It supplies the saved
person's canonical home jurisdiction and World/current date to the existing
BudgetEconomyWorkspace. The original RECOVERY25 patch and reader remain intact.
There is no second panel, fiscal writer, source acquisition or transit work.

A reported that the root was already composed in frozen `27b8eb9c`/`42b76858`
and that the original three browser checks passed. This owner does not repeat
or override that root integration. A should consume only the additional proof
below and the matched fixture repair where not already included. In particular,
never copy this receiver's whole PlayerGame over A's composed D/Civil root.

## Additional net return

- `tests/e2e/recovery25-budget.spec.ts`: a normal age-38 Lexington start using
  the deterministic generated shared-home seed `delivery28-budget-pending-0`.
  No custom household, injected person, source-date override or fiscal authority
  is supplied. People offers the actual household-obligation conversation and
  Listen remains pending. Budget opens by ordinary menu navigation; keyboard
  Back returns to the same actor, topic, line, choices and pending Listen. After
  Keep and normal reopen, the same established conversation is opened through
  People and verified again. Pointer Back also returns unchanged. Complete
  serialized World payloads remain byte-identical, including time/history.
- `src/presentation/economic-context-panel.test.ts`: explicit zero remains a
  visible point with `0 USD minor units`, while missing values have no point.
  Existing sparse-gap and exact USD/EUR/fractional checks remain intact.
- `src/presentation/budget-economy.test.ts`: retain A's already-authored explicit
  `us-ky-general-assembly-v1:house` fixture key. The legacy implicit-office test
  fails under current production filing predicates; production keeps requiring
  a selected office. No authority check was relaxed.

The added proof is an ordinary private citizen, not an office fixture. The
seated-reader unit test remains a separate, explicitly labeled scenario.
The new pending-conversation check uses a 1440×900 desktop viewport. A's bounded
90-second creator/read/Back/Keep/reopen driver is retained. The initial equivalent
1280×720 journey also passed; CSS text-transform capture was corrected by
comparing DOM text consistently with Playwright's full toHaveText assertions.

The source geography crosswalk, record classes, money units and renderer are
unchanged from the accepted #222 leaf.

## Verification and boundaries

The new pending-conversation journey passed, alongside the existing three
Budget checks and 30 focused tests on this current-main receiver. Typecheck,
lint, art validation, inventory and QA passed. The final frozen exact-source
checks are returned with the publication head. No full local aggregate was
started while A's combined frozen validation was running. Initial focused/browser/codegen attempts encountered shared-host ENOSPC
before meaningful execution. Only this owner's earlier disposable Budget dist
output was removed; failure logs are retained. The first conversation driver
used a solo normal seed and correctly found no household topic; it was replaced
with an actual normal generated shared-home seed, without weakening assertions.

The historical #222 repository CI succeeded; its failed browser shard listed
News narrow-layout and legacy P2R driver cases, not a failing Budget assertion.
This is historical CI evidence, not a green current aggregate claim. A owns the
combined frozen aggregate and final shared-root/main integration. This owner
performs focused Budget checks and ordinary browser proof, not independent
human acceptance of its own work.

Implemented isolated integration and demonstrated normal-route proof are distinct
from containment on main, a shipped client, and human visual acceptance. A's
next result is to preserve its already-mounted root, add the current continuity
proof, and publish/land the identified safe Budget composition when its gates
permit. Budget does not wait for transit.

## LEARN and architecture disposition

Confirmed compatible: one existing reader/provider/graph seam; canonical date
and geography, units, missingness and append-oriented World/history stay intact;
React remains outside the simulation. Browsing uses no World writer. Root
reconciliation preserves newer consumers rather than replacing a whole file.

A normal start can legitimately be solo. A pending-household browser proof must
name a deterministically generated shared-home life, and separately keep the
unmatched/missing and zero negative controls. A custom household or increased
timeout cannot stand in for that actual route.
