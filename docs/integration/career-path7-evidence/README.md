# Career source-to-work evidence

Published initial donor: 65f0d29f82ea55029a8e13cd227a1f68e3489d34, with explicit unmerged LIFE cb3a97ae dependency. Follow-up controls and normal-route test are carried by this branch's subsequent delta.

Executed checks:

- Six focused tests pass: full locked-source/runtime replay; corrupt digest, absent artifact/file and unknown-rights refusals; qualification and invalid offer controls; refusal/acceptance/start-date; exact 240-minute shift, duplicate completion, due-date pay; actual 12-session training then employer switch; resignation and same-world serialization.
- `npm run source:validate` passed across 16 production domains; existing government-finances no-missingness warning remains unchanged.
- `npm run source:replay` passed byte-identically.
- Production build passed with the existing chunk-size warning.
- Required art validation passed, inventory remained 329 items, and QA generated successfully.
- Focused ESLint passed after correcting two type-import declarations.

UI owner reported applying the feature-local donor and LIFE-panel patch, preserving its existing combined LIFE corrections. Exact UI publication and executed normal-player browser evidence remain pending. `tests/e2e/career-path7.spec.ts` is a concrete normal-route contract, not a claimed pass. LAND holds the finite heavy/browser slot for #141 and will explicitly release it. No fixture browser proof is substituted.

Known limits: three authored employer contexts, not every source occupation; no public hiring, legal licenses, journalism provider, named supervisor quality verdict, or historically calibrated occupation availability. Source dates/geography, authored contracts and fictional employers remain distinct. Human visual and independent acceptance have not been granted.

## Normal-route correction after UI execution

UI owner ran the normal spec on ce2df91f9ad81a8d6015b1e8c1ce1499f815e35f. It failed at beginning accepted work. The trace's after@call@90 snapshot explicitly reports “Resolve your current calendar commitment before waiting.” The first wait returned the unchanged World; the spec ignored that refusal and attempted to begin before the next-day start date.

The bounded correction asserts that refusal, visits normal Day, carries out the existing venue activity, verifies its completion, returns to Work, and asserts successful waiting before beginning. No runtime, time, eligibility, or source changes. This corrected spec has not yet been browser-executed; UI owns the current browser slot. Browser failure remains open until rerun.
