# PEOPLE41 six standing families — A handoff

Final runtime source: `1d4892257c50d2fcc6b1e16e27915a969ea397d9`.
Branch: `codex/people41-a39-handoff`; isolated workspace `/private/tmp/pg-people41-a39`.
Private identified Play: http://127.0.0.1:5288/?art-preview=candidate
Identity: http://127.0.0.1:5288/__dev/identity
A39 source32be6a1c plus B historical36/40, six-family41 and G standing correctionbb0636. A's existing5279 delivery remains unchanged. No future seated or kit increments are included.

## Delivered

Six standing body families: masculine/feminine average, lean and heavy. Two completed average people preserved; four independently generated adults. Each family has trousers, footwear, head/hair and actual short-/long-sleeve garments. Seventy-eight registered modular components, generation10 signature `csig_a775b44da481cca6`. No flattened person in the runtime registry.

Provider: actual OpenAI built-in `image_gen`; model identifier not exposed. Fourteen successful calls for the four new adults, sleeve alternatives, skin/scalp inputs, repaired polo and average female long sleeve. Exact calls in `generation-receipts.json`. All18 raw source PNGs remain1024×1536 full-colour. Current primary style1SBz4LrIX2XSJrsDprorQ7P8tjNZMCYY0 only; source1672×941, native audiencecrop supplied as actual image reference. Body references provide structure only. Old tableaux were not recovered for generation.

`raw/` holds unmodified masters; `extracted/` genuine RGBA; `families/` individual compatible PNG/SVG parts. `comparison.html` compares current style, raw and imported output, actual game-size swaps and portraits. `game-fit-matrix.png` is assembled from actual browser captures. `source-fit-matrix.png` shows the authored modular assemblies.

## Corrections and preservation

The fitted blue polo fixes the front cuff/waist mismatch. Sleeve extraction no longer stops at the old waist crop. Same-canvas collar partition, head-only masks, minimal painted-edge overlap and jaw-relative skin overlap preserve hands, arms, neck and waist. No body shrinking, heavy-ink processing, palette reduction, save rewrite, new renderer, paid API or runtime AI.

Three independent painted identities per presentation are compatible with each structural body. Face and matching hair remain separate components; body selection retains their identity suffix. Eighteen head/body pairs pass a direct neck-alpha continuity guard. Painted complexion is fixed source colour, with no parametric face/iris/complexion promise. Matching hairstyle compatibility is explicit; broader consumer controls belong to KIT.

Average body/head/hair/trousers/shoes PNGs remain byte-identical to PEOPLE40; original average shirt unions also match exactly. Generation7/8 runtime source and components are unchanged, and generation9 ledger is frozen. `freeze-receipt.json` records protected paths and signatures. Existing local3dfc5dcf source was read-only.

## Verification

Both focused groups PASS on the exact final source above, with a clean tree and zero browser errors:

1. All6 structural families were selected through actual Body controls. All12 short/long garments were swapped through Shirt style. Every row preserved its head/hair, trousers and footwear across the garment change; its actual portrait head/hair hashes matched the full-body rendering. Independent identity suffixes persisted when changing build. Source checks cover true alpha, full visible bounds, immutable average pixels and all18 neck pairings.
2. Two disposable ordinary lives (Standing One and Standing Two) were created through the normal creator, edited, saved, reopened and matched their previous component/paint hashes exactly. Same-build generation8 control still draws ep36, never ep40/41. The clicked NPC's room and Talk portrait head/hair hashes match by stable person ID. Pointer and keyboard actions were exercised; both1280×860 and1200×720 Talk captures retain the full standing actor.

Receipt: `qa/report.json`; exact tested source digest `f32d53d06f2f16c46b0587276a73c5e76466741c8bbfb79612bcf7a3a78309b1`. Earlier failure receipts preserve obsolete harness selectors/aggregate participant diagnosis; they are superseded by the final successful receipt. No runtime repair was made for that selector issue.

Normal changed-file hygiene: changed TypeScript lint and typecheck passed; final production build passed. Existing prepared/room contract group:7tests passed. `validate:art`, `inventory:art`, `qa:art` ran once for this batch; generated inventory churn was saved externally then restored. Existing old duplicate-hash notes remain. No full-suite-per-asset testing or public release.

Technical completion is not owner style acceptance. The figures follow the simplified illustrated direction, though some garment contours remain stronger than the seated audience. No remaining missing-family, sleeve, alpha or modular-swap blocker is claimed. Human visual acceptance remains with the owner/A; it does not reopen the finished standing batch or gate it on future seated/wardrobe work.

## Learn

Check each imported identity against every compatible structural neck, rather than only its original dressed source. Retain original painted edge overlap through reduction; clip garments by their full sleeve extent. Browser identity assertions must select one stable person ID rather than aggregate all conversation portraits. These guards are encoded in `check-source.py`, the final builder, and `check-playable.ts`.
