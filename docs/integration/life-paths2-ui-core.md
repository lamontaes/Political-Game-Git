# LIFE-PATHS2 handoff to UI-CORE-RELEASE

Integration owner: UI-CORE-RELEASE, PR #133, `claude/ui-prototype-01-build-iepvtr`.
Feature owner: Codex LIFE-PATHS2, branch `codex/life-paths2`.

`src/player/LifePathsPanel.tsx` accepts canonical `world`, `onWorldChange`, and
optional `transitionHandlers`. Mount it in the ordinary-day surface. The exact
unapplied patch beside this note adds the adapter to `PlayerGame` and composes
`LIFE_PATHS2_HANDLERS` into the shared campaign/future transition registry. QUAL
has been notified of the registry hunk. Apply or adapt it once in the owning
integration branch; do not create a second pay loop.

Expected player path: ordinary day → Education and work → choose supported path
→ for study, schedule and attend; for ordinary work, Perform work or skip time
through the authored window. Interrupt/return/leave and review progression remain.
Recruitment uses the known-person selector, terms, offer response, start, assignment and end.
Money is posted by canonical future transitions, including when the player uses
another calendar surface after leaving this panel.

The extra HTML entry is an isolated synthetic browser fixture with its own save
namespace. It is not normal-player navigation or release acceptance. Human visual
acceptance remains pending. Public-office hiring and real institution program
capabilities remain source-blocked as detailed in the system document.
