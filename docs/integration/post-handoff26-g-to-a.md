# G #225 frozen delta for A

Continue [#225](https://github.com/lamontaes/Political-Game-Git/pull/225), not #212/#214 separately. Both donor commits are preserved in its ancestry. Reconciliation incorporates main `22c4deadbd54f0b5799b40b8671d792831befcbd`; only generated prose README, coverage candidates/report and review packet conflicted. Sanctioned regeneration resolves them on combined source. `PlayerGame` merged cleanly; its newer Personal/desktop composition is retained. No scene, anchor, tier, original art or existing screenshot/video is rebuilt.

The completed storefront route remains `?art-preview=candidate` → ordinary campaign filing → afternoon phone session → close Work. `?view=location-review` retains storefront, pavilion and exact IMG_5202 press-room shared-compositor staging. [Existing proof](../agent/evidence/g-location-review/README.md) remains historical evidence of its exact served digest, not a claim of this reconciliation's execution. Production source approval and human visual acceptance remain pending. Storefront restored unreleased status, pavilion/press candidates and executive exclusion remain intentional.

## Precise normal press-venue connection still required

The normal `PressWorkspace` arrangement handler passes `location: { locationKey: 'press-planned:<request-id>', label: place.trim() }` to `arrangeAcceptedPressInterview`. That stable request-specific planning key and arbitrary user label establish no physical venue. `arrangePressInterview` faithfully copies this location to a confirmed scheduled activity with the subject/reporter IDs. Neither opening Press, arranging an exchange, naming the typed place “Press room”, nor listing invited participants proves presence in IMG_5202. Written/phone exchanges also must not inherit this physical room. Existing interviews are not a producer for a group press briefing.

A/the existing press producer owner must provide an explicitly supported physical briefing-room venue identity and suitable in-person activity through the existing arrangement/producer contract. The venue must have a canonical location key supplied by actual venue authority, known jurisdiction/access and actual participant records. Do not invent a key or infer the room from a label, request prefix, filename, office eligibility or generic interview channel. A supported individual in-person exchange needs explicit venue suitability; it is not authority to synthesize a briefing or audience.

The current scheduler hardcodes `access: { kind: "office" }`; the real venue owner must reconcile legitimate access rather than granting reporters office eligibility to draw them. `contextFromActivity` currently intersects the narrative scene's people with completed participants. A's adapter must project confirmed real attendees into that context when needed; the activity roster is not an invitation to synthesize people, and a binding alone does not make an absent reporter visible.

Then G/A can add exactly one binding for that established key to `press-briefing-room-candidate`. The scene currently has `presentationStatus: development-fixture`; the production resolver also requires a production scene and a released asset. Thus a key mapping alone is insufficient. Keep the finite development allowlist and explicit candidate-mode policy for preview; production requires separate same-source acceptance and the normal scene-release contract. Do not change manifest approval to make the connection pass.

Reuse `completePressInterview` and the canonical activity/clock path; it requires confirmed response, performs the activity once and records completion. `completedActivityHere`/`resolveActivityVenueScene` accept only matching completed presence at the current completion instant, access permission and a non-travel venue. Pending requests, planned times, cancellation, blocked commitments, off-site exchanges and later clock movement remain negative controls. Person painting must project only actual established presence from the canonical producer, including the reporter only when established; never add gallery actors or invited people to World history.

D is needed only if the chosen real venue lacks the already-supported journey binding. The small D adapter would resolve that venue's existing canonical destination/access and disclosed travel time/cost through the current Attend journey, recording arrival before attendance and refusing without invented arrival. No movement implementation is justified while there is no actual venue identity. A mounts the established producer/binding; G owns art/data glue, not new attendance or time semantics.

Required proof for the later connection: actual normal arrangement/attendance controls to the exact canonical venue; recorded location/time/participant evidence; read-only browse/return; refusal/pending/written/off-site/no-access/no-arrival and later-time gates. Use existing room pixels and compositor. Candidate proof remains labeled; production release and human visual acceptance are separate.

## Reconciliation check receipt

Closed checks: sanctioned corpus regeneration/check, explicit intake-family list, figure-framing measurement report and asset-bank inventory. Full suite: 331 files, 4652 tests passed/2 skipped with two workers and binding permission. Format/lint/type/release/source/replay/education/municipal checks passed; build hit ENOSPC then prescribed build retry and remaining demo/art gates passed after G-only disposable cleanup. Required art validation/inventory/QA passed. Final head and sequential exact-commit G browser checks are supplied in the PR return. No broad-suite green or independent owner art approval is inferred. Integration into main and the installed executable remain A's separate delivery.
