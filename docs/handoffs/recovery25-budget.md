# RECOVERY25 Budget → A

Receiver: A, task `01a095e9-50d7-7923-9fb8-012c8964431f`
(`Finish MORNING23 A landing UI`). A retains shared-root integration and main merge.

Publication: [PR #222](https://github.com/lamontaes/Political-Game-Git/pull/222),
`codex/recovery25-budget`. The original implementation is
`6831e6fc43ba58c0b50479402ae39dff477d6472`; its successor delivery commit preserves
that parent and adds only the contained graph repair, proof, release declaration,
required generated prose artifacts, and this handoff. Read the PR head for the
exact successor SHA rather than substituting the historical original head.

## Current mount

[The three-file Politics adapter](recovery25-budget-politics-mount.patch) applies
cleanly both to accepted main `215b3e90d163ed3344dbbd36bfaa621501bd67f8` and A's
read-only local checkout `18b823e28d2505fe4434ca67936d9f696b1889ca` at
`/private/tmp/pg-post-handoff26-a.zXOM6K`. A's matching published branch was
`codex/post-handoff26-a` at `c0b0ba33b7feaed8f75adf9897c82dd9862e3132`.
These are inspected snapshots, not claims that the surface has landed on main.

The adapter adds the Politics destination/group and passes the saved person's
canonical `homeJurisdictionId` and complete World into `BudgetEconomyWorkspace`.
The read model uses `world.currentDate`; provider bindings remain exact authored
place crosswalks. It introduces no time or fiscal writer. A may compose this
minimal seam into newer Politics navigation while preserving the leaf props.

Consume the feature source from the PR, or apply its source-only delta from
`6831e6fc^`; do not overwrite current PlayerGame with an old whole file. The
original commit's generated prose packet conflicts with newer composition and
must be regenerated once in A's final composition. Do not choose the old packet
as the resolution. No donor merge or main merge occurred in this continuation.

## Completed checks

A disposable composition at `/private/tmp/pg-recovery25-budget-current-proof`
contains current main plus the published source delta and contained repair.
Its tested source fingerprint is
`a8e0c06a3efa76b1d261fdbc34dd1b58aa0439eb264cd4d67c41fd6630382da7`.

- 42 focused reader, source-replay, graph and shell tests passed (eight files).
- Three Budget Playwright proofs passed on an identified isolated server at 5439.
- Repaired-source typecheck, lint and production build passed. The first repaired
  build retry was needed after a shared-disk ENOSPC failure; only this task's
  disposable dist output was removed. The successful exact-source receipt is
  `repaired-build-retry.json`; the failed attempt log is retained separately.
- Actual keyboard cluster/destination activation, menu dismissal, News replacement,
  Back restoration and Close were exercised. The durable browser proof includes
  keyboard activation/replacement/Back plus pointer navigation and disclosures.
- Supported-date fixture graph/table metadata, normal public reading, unmatched
  Carson City unavailability and save/reload World-byte purity passed.
- The focused seated-reader test reaches a canonical active member seat through
  the existing route; public browsing remains authority-independent. No operative
  fiscal predicate or writer was changed.
- Current-composition `validate:art`, `inventory:art` and `qa:art` passed. Inventory
  has 1,872 items; duplicate-hash warnings were retained without changing artwork.

Exact-source local receipts/logs: `/private/tmp/recovery25-budget-current-receipts`.
Browser identity and results: the disposable checkout's ignored
`test-results/runs/recovery25-budget-repaired-current`.

The normal route screenshot shows honest date-based unavailability, not synthetic
history: [normal current-main mount](../agent/evidence/recovery25-budget/normal-current-main.png).
The deliberately authored supported-date fixture demonstrates two isolated fiscal
observations and exact-unit tables: [sparse fiscal proof](../agent/evidence/recovery25-budget/sparse-fiscal-current-main.png).
These fixture amounts are not sourced government budget claims.

## Contained defects corrected

A singleton polyline drew no visible mark, including an observation separated by
missing periods. The existing renderer now draws a marker for each singleton
segment; it does not connect across gaps or invent more periods.

The Exact values formatter rounded monetary and fractional values. Minor-unit
records now display their supplied numeric amounts and units, without conversion
to whole dollars; fractional table values retain the supplied precision.
Regression checks cover sparse gaps, entirely missing values, USD/EUR minor units
and fractional percent/dollar records. Existing record classes and geography labels
remain intact.

The original head's CI repository job stopped at `release:check`, before aggregate
tests, because the feature lacked a declaration. The successor adds
`recovery25-budget-economy` with minor impact; it does not bump a version or modify
accepted patch notes. Older aggregate timeout reports remain historical evidence,
not a passing current aggregate claim.

## Acceptance and next user result

Implemented and handed off; source-only current-main mount is browser-proven.
Not integrated into accepted main and not human visually accepted. A's next step
is to consume the leaf and adapter, regenerate the combined prose corpus, run the
composition gates, and publish an identified build with the reachable Politics
route. Preserve unavailable dates/areas, exact units, table alternatives, and
existing authority predicates. No new panel, fiscal engine or main-merge authority
is included.

## LEARN

A metadata-only graph assertion can pass while a singleton observation remains
invisible. Retain the browser marker/table assertions alongside the missing-period
negative control. Declare release impact before publication; never substitute a
version bump for the repository's declaration contract.
