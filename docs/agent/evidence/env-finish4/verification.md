# ENV-FINISH4 verification in progress

Existing lane: PR #142, `codex/env-all1-transfer`. Source recovery and original
masters remain preserved; no original checkout was changed.

## Published county checkpoint

`fd2eea80b2b66fc4853767b00a7981a71188380c` connects all 3,222 accepted county
identities to the existing life-place provider. Three focused files / 19 tests
and TypeScript passed. All three required art commands passed. UI #144 copied
the exact adapter and applied explicit county/town-unspecified labels locally;
its normal-route proof is still pending on the combined checkpoint.

## Prop review

`14b81ed85e46cf1b275f19fb953b2cccf63995f7` exposes all 12 preserved coarse prop
crops in the existing gallery. The former blanket inseparability claim was
incorrect: individual and grouped crops already exist. Source bytes and crop
hashes are unchanged; candidate status is unchanged.

The isolated gallery at port 5362 passed actual pointer activation at 1440px
and Enter activation at 960px, then hide/removal. Both runs showed 12 crops,
explicit nonrelease labels and no page errors. Screenshots and reproducible
script are adjacent. The first proof incorrectly asserted 10 crops; corrected
proof compares against the actual intake report. This is gallery evidence,
not normal-play prop release or human acceptance. The owned server was stopped.

## Occlusion repair, awaiting browser slot

`SceneBackdrop` previously ignored every registered occluder and placed people
in a separate depth context. Released masks now share the people depth stack;
names remain readable above physical layers. Debug rectangles remain nonpainting.
The existing schema additionally accepts explicit visual-estimate vector clips
of the currently painted plate. The community lectern clip traces its board,
stem and base; microphone and floor shadow are excluded. It reuses the same
painted URL and camera transform and creates no new raster or release.

Scene/schema/source/occlusion focused checks: 106 tests pass. TypeScript passes.
The actual corpus generator measured 52,526 literals in 355 files; the measured
pin is updated without changing template identities. Final browser and full
gate results are not yet claimed. Queue: LIFE/EXEC → OPENING → DEV → LEG →
ENV short proof → PEOPLE → MUNI. No competing full/build/browser run is active
in this ENV lane.

## Exact remaining connections

- UI #144 owns actual MUNI callback and county setup/save proof. The existing
  community activity connection and its earlier Save/Continue evidence remain.
- MUNI owns additional source-attributed venue bindings; generic chamber art
  cannot assert real architecture, member count or seating assignments.
- LEG confirmed no published completed located hearing, floor or workroom
  attendance adapter. Opening a docket cannot establish presence.
- OPENING published its walk shim in #150 / 649f10b, delegating to the preserved
  ENV travel adapter. Its five-minute route is authored, not measured geography.
- No city/county relationship crosswalk exists in the two Gazetteer identity
  corpora. No town, municipal membership, route or duration is inferred.
- Workroom alpha, hall body calibration, supported seated/child pixels and
  candidate art decisions remain distinct from released empty-room coverage.
  No painted crowd becomes a canonical participant.

## Renderer browser correction

The first depth fixture passed its DOM assertions but visually exposed darker
repainted furniture. That was a real failure, not visual acceptance. The repair
subtracts admitted alpha/vector silhouettes from each person only when the
occluder is in front of that person's anchor. The room is not repainted; labels
remain unmasked. The red blocks in this fixture are deliberately oversized
rendering controls, not approved people or evidence of actual attendance.

Office alpha and community lectern vector checks passed at 1440 and 960 pixels,
with pointer/Enter activation and no page errors. Screenshots visibly confirm
occlusion without the darker duplicated furniture. The microphone and shadow
remain explicitly outside the authored lectern clip. Real-person fitting and
normal MUNI/county proof are separate outstanding checks.

Corrected corpus measurement: 52,533 literals / 355 files. TypeScript and the
three occlusion tests passed after the repair.
