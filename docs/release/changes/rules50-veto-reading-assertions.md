---
id: rules50-veto-reading-assertions
impact: none
---

Nothing changes for the player. The check that holds a state's veto override to its own constitution was rewritten so it cannot quietly stop checking: it no longer names which states are unplayed, and it now pins the source of each read threshold rather than only its fraction.
