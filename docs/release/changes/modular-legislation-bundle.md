---
id: modular-legislation-bundle
impact: none
---

Engine-only: the legislative compiler now composes a multi-subject measure from
independently-authored components — amend and repeal at a named provision
revision, namespaced cross-references, per-currency fiscal separation — behind
the existing single-family filing path, with no new player-facing entry point.
Nothing in shipped play reaches a bundle yet, so there is nothing to tell a
player until a docket surface offers one.
