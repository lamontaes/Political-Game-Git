---
id: pt3-mac-play-launcher
impact: none
---

Adds `scripts/play-mac.command`, a double-clickable macOS launcher that builds
a play copy in its own detached worktree, refuses to take a port from a running
server, stops when the dependency install fails, and opens only the game's own
address. Developer and review tooling; the shipped game is unchanged.
