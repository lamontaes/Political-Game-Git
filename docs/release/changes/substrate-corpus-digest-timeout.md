---
id: substrate-corpus-digest-timeout
impact: none
---

Raises a test's own per-test timeout so hashing EDU-PATH7's larger corpus set
doesn't trip the default under CI load. No behavior change and no player-visible
effect.
