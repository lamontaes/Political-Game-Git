---
id: state-voting-off-the-substrate
impact: none
---

The state voting survey the opening shows is now read entirely by browser code. It used to reach the Node-only source substrate for its projection, which the architecture forbids the browser app from doing. The figures, the dates and the wording a player sees are unchanged.
