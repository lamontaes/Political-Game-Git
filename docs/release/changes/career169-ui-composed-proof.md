---
id: career169-ui-composed-proof
impact: none
---

Adds a real reachability check to career-path7.spec.ts so it skips with the
actual reason on a tree that lacks UI's composition, instead of hard-failing;
adds evidence of the proven composed journey. No behavior change and no
player-visible effect.
