---
id: appearance-recipe-v2
impact: none
---

Development-only. Appearance recipe v2 keeps a person's face in the same
measured skin as the body carrying it, and a life started now is declared under
it; but the coherence needs a catalog with measured tone, and the shipped
production catalog carries none — it resolves `complexion-unmeasured` and falls
back to the v1 draw. Nothing a player sees changes until a measured bank is
promoted. No existing save is repainted: an appearance is written at creation
and the default recipe does not move.
