---
id: click-list-ordering-320-before-325
impact: none
---

Documentation only. The morning click-through index now names the one place in
the list where order matters: #320 removes the source references a player was
reading on a refusal, and #325 is the screen-path check that proves the refusal
arrives clean. #325 carries no sentence fixes of its own and is red on main and
green on #320's head, so clicking it first would land a gate against code that
is not there yet. The index also refreshes the merged count against
`git log --merges` rather than memory, and drops a pull request that has since
merged from the list of drafts still moving. No game, build or release change.
