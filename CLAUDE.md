# Our Civic Duty — Claude Code project bridge

Read and obey `AGENTS.md`. This file is only a short bridge to the authorities
that actually hold current truth; it is not a place for domain detail.

1. Read/obey repository `AGENTS.md`.
2. For current product/ownership/task truth, read the project's canonical
   Google Drive chain in this order:
   - `00_READ_FIRST — CANONICAL READ-WRITE PROTOCOL`
   - `01_CURRENT_CANONICAL_INDEX — PROJECT STATE AND SOURCE PRIORITY`
   - `04_CURRENT_DECISION_CHANGE_AND_TRACEABILITY_REGISTER`
   - `00_CURRENT_ASSIGNMENT_BOARD`
   - `00_STAGING_INDEX`
   - the current handoff named by the canonical index, when a
     continuation-level read is necessary.
3. Live GitHub/repository exact state overrides remembered SHAs/status.
4. One writer per overlapping branch/PR. Do not merge your own consequential PR.
5. Never treat green tests as human visual/play acceptance.
6. Before substantial work, `git fetch`, verify worktree/branch/base, and
   consume the owning packet/domain authority instead of rediscovering the
   project.
7. Keep durable task progress in the owning plan/completion file for long runs.
   Use `/clear` between unrelated jobs; `/compact` for the same crowded job
   after checkpointing; `/goal` only with an objective verifiable finish
   condition.
8. Never duplicate the World/simulation/source/art systems to make a task
   easier. Reuse accepted seams; fail closed when required truth/art/source is
   missing.
9. When Drive current controls and an old repo handoff disagree, current
   canonical Drive + live repository win.
10. Do not make this `CLAUDE.md` large. Domain detail belongs in
    packets/docs/skills.
